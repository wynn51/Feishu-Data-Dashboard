# 月度数据中间表生成提示词与中文逻辑脚本

本文档以当前工作簿为准，用于指导自动化程序生成：

- `数据中间表/月度数据中间表/月度数据中间表_YYYY年MM月.xlsx`
- `数据中间表/在途批次表/在途批次表_YYYY年MM月.xlsx`
- `数据中间表/净需求计算表/净需求计算表_{{站点_批次}}_YYYY年MM月.xlsx`
- `结果表/运营大盘数据监控表.xlsx`

本文档只描述提示词和中文执行逻辑，不直接提供代码。若旧版说明与本文件冲突，以本文件为准。

---

## 一、可直接使用的提示词

请你作为 Excel 自动化工程师，接收根目录 `ROOT` 和销售归属月 `M`，严格按照本文档生成在途批次表、分站点净需求计算表、月度数据中间表，并更新结果表 `运营大盘数据监控表.xlsx`。

必须复刻原工作簿中的文件名、Sheet 名、字段名、数据粒度、字段顺序、样式和公式逻辑；中间表公式必须保留为 Excel 公式。所有公式在写入后必须调用本机 Microsoft Excel 全量计算、保存并关闭，再读取公式缓存值更新结果表。

### 输入参数与月份变量

| 参数 | 含义 | 示例 |
|---|---|---|
| `ROOT` | 根目录，内部包含 `源数据`、`数据中间表`、`结果表` | `<ROOT>` |
| `M` | 商品销售统计的销售归属月，格式 `YYYY-MM` | `2026-05` |
| `F` | 月度中间表快照日，`M` 的当月 1 日 | `2026-05-01` |
| `BATCH_F` | 在途批次表日快照日，`M` 的下一自然月 1 日 | `2026-06-01` |
| `DATA_MONTH_CN` | 销售归属月中文标签 | `2026年05月` |
| `DATA_QUARTER_CN` | 销售归属月所属季度 | `2026年第二季度` |
| `PLAN_M` | A1/A2 计划目标辅助引用月，与销售归属月 `M` 相同；主净需求窗口从 `M+1` 开始 | `2026-05` |
| `PLAN_MONTH_DOT` | A1/A2 计划目标辅助字段标签，与 `M` 对应 | `2026.05` |

> 统一月份规则：`A1类目销售数据表`、`A2类目SKU销售监控表`、`B1运营类目销售表`、`B2店铺类目销售表` 的销售月份全部使用 `M`，对应月份和季度分别固定为 `DATA_MONTH_CN`、`DATA_QUARTER_CN`。分站点净需求表的主目标窗口使用 `M+1` 至 `M+6`；同时在净需求表末尾生成 `M` 的类目目标和 SKU 目标辅助列，供 A1、A2 的计划目标读取。B1、B2 不读取 `PLAN_M` 或 `PLAN_MONTH_DOT`。

### 输入文件路径

| 文件类型 | 日期口径 | 路径 |
|---|---|---|
| 商品销售统计 | 销售归属月 `M` | `ROOT/源数据/千易销售数据/商品销售统计_M.xlsx` |
| 库存查询（月度中间表） | 执行月月初快照 `F` | `ROOT/源数据/千易库存数据/库存查询_F.xlsx` |
| 库存查询（在途批次表） | 次月月初快照 `BATCH_F` | `ROOT/源数据/千易库存数据/库存查询_BATCH_F.xlsx` |
| 收货中入库单（月度中间表） | 执行月月初快照 `F` | `ROOT/源数据/千易入库单数据/入库单明细_收货中_F.xlsx` |
| 新建入库单 | 次月月初快照 `BATCH_F` | `ROOT/源数据/千易入库单数据/入库单明细_新建_BATCH_F.xlsx` |
| 飞书入库单跟踪表 | 次月月初快照 `BATCH_F` | `ROOT/源数据/飞书人工维护数据/入库单跟踪表/入库单跟踪表_BATCH_F.xlsx` |
| A未发数据表 | 对应月份 | `ROOT/源数据/飞书人工维护数据/A未发数据表/A未发数据表_YYYY年MM月.xlsx` |
| 映射表 | 固定 | `ROOT/源数据/映射表/SKU、店铺、仓库映射表.xlsx` |
| 运营大盘数据粒度表 | 固定 | `ROOT/源数据/映射表/@数据粒度表_运营大盘数据监控表.xlsx` |
| 净需求计算表模板 | 按站点批次 | `ROOT/源数据/净需求计算表/@净需求计算表-{{站点模板名}}.xlsx` |
| 分站点净需求计算结果 | 销售归属月 `M` | `ROOT/数据中间表/净需求计算表/净需求计算表_{{站点_批次}}_DATA_MONTH_CN.xlsx` |

净需求计算表不得硬编码为某一个站点。生成阶段按 `菲律宾、马来、泰国、印尼1、印尼2` 分别选择对应模板；月度中间表汇总阶段只读取本次执行生成且经 Excel 全量计算保存后的 `菲律宾、马来、泰国、印尼1` 四份结果文件，明确忽略 `印尼2`，不得直接读取源模板缓存。

### 输出文件

- `ROOT/数据中间表/月度数据中间表/月度数据中间表_DATA_MONTH_CN.xlsx`
- `ROOT/数据中间表/在途批次表/在途批次表_DATA_MONTH_CN.xlsx`
- `ROOT/数据中间表/净需求计算表/净需求计算表_{{站点_批次}}_DATA_MONTH_CN.xlsx`
- `ROOT/结果表/运营大盘数据监控表.xlsx`

### 月度中间表模板策略

1. 以现有 `ROOT/数据中间表/月度数据中间表/月度数据中间表_2026年05月.xlsx` 提炼固定结构和样式，包括 15 张 Sheet 的名称与顺序、字段名与字段顺序、列宽、行高、冻结窗格、筛选、字体、填充色、边框、对齐、数字格式及公式样式。
2. 上述结构、样式和公式模板必须固化在 Python 代码中；正式执行时不得依赖历史月份的月度数据中间表存在。
3. 即使 `ROOT/数据中间表` 中不存在任何历史月度中间表，脚本也必须能够从源数据、粒度表和本月 `菲律宾、马来、泰国、印尼1` 四份净需求计算结果独立生成完整工作簿。
4. 现有 2026 年 05 月文件仅作为开发校准依据，不得在运行时被覆盖或修改。

### 强制要求

1. 文件名、Sheet 名、字段名必须与当前工作簿完全一致，不允许同义改名、大小写变更、空格增删或符号替换。
2. 不能只依赖 worksheet dimension、`max_row` 或 `max_column` 判断千易源文件是否为空；必须实际读取数据行。当前千易销售文件可能把 dimension 错写为 `A1`。
3. 多层表头必须按真实业务表头行识别，不能固定取第 1 行。
4. 公式字段必须保留 Excel 公式；由程序新增的公式列使用浅蓝色背景。
5. 脚本自身执行映射时，查找不到映射不得新产生 `#N/A`，应按字段规则返回空白、`0` 或“未找到目标”，并在日志中输出未匹配数量和样例键。唯一例外是分站点净需求表被导入字段中已经存在的 `#N/A` 缓存值，必须原样写入 `净需求-目标表`。
6. 所有除法公式必须处理分母为空、文本或 0 的情况，不得生成 `#DIV/0!`、`#VALUE!`。
7. 结果表不得包含失效外部工作簿引用，例如 `[1]产品动态分级表（初稿）`。
8. 月份字段必须整列写入同一个月份值或使用绝对引用公式，禁止用 Excel 序列填充导致每行递增一天。
9. 月度中间表经 Excel 全量计算并保存后，才能更新结果表；结果表保存时间必须晚于中间表公式激活完成时间。
10. 每个 Sheet 完成后必须阶段性 `print` 输出日志，包含 Sheet 名、数据行数、字段数、公式列、未匹配数和错误数。
11. 在途批次表处理 A 未发数据时，菲律宾 SKU 必须先删除开头的 `HSTEST-`；A 未发数据保留在独立 Sheet，并且只能向 `入库单表` 追加一次。

---

## 二、执行入口与前置校验

### 2.1 执行入口

1. 接收 `ROOT`、`M`。
2. 校验 `M` 格式为 `YYYY-MM`。
3. 计算 `F`、`BATCH_F`、`DATA_MONTH_CN`、`DATA_QUARTER_CN`、`PLAN_M`、`PLAN_MONTH_DOT`；其中 `PLAN_M=M`，`PLAN_MONTH_DOT` 为 `M` 对应的点分月份标签，只服务于分站点净需求辅助列和 A1/A2 计划目标字段选择。
4. 校验或创建 `ROOT/数据中间表`、`ROOT/结果表`。
5. 输出所有输入、输出路径和月份变量。
6. 先生成 `在途批次表_DATA_MONTH_CN.xlsx`。
7. 使用各站点现有净需求工作簿作为只读模板，生成并计算 `数据中间表/净需求计算表/净需求计算表_{{站点_批次}}_DATA_MONTH_CN.xlsx`；源模板不得被脚本覆盖。
8. 分站点净需求表 Excel 全量计算并保存后，再生成 `月度数据中间表_DATA_MONTH_CN.xlsx`。
9. 月度数据中间表 Excel 全量计算并保存后，最后更新 `运营大盘数据监控表.xlsx`。

### 2.2 源文件校验

1. 校验所有必需文件存在且包含有效数据行。
2. `商品销售统计_M.xlsx / sheet1 / 时间范围` 必须覆盖 `M`，例如 `M=2026-05` 时应为 `20260501-20260531`。
3. 月度中间表的库存和收货中入库单文件名日期必须等于执行月 1 日 `F`；在途批次表的库存、新建入库单和跟踪表文件名日期必须等于次月 1 日 `BATCH_F`。例如 `M=2026-06` 时，月度中间表读取 `库存查询_2026-06-01.xlsx` 和 `入库单明细_收货中_2026-06-01.xlsx`，在途批次表仍读取 `BATCH_F=2026-07-01` 对应快照。
4. 每个分站点净需求计算表模板必须包含 `净需求表`、`计划目标表`、`货值情况`、`千易库存数据`、`千易入库单_收货中`、`销量占比`、`SKU信息表`、`仓库信息表`、`入库单信息`、`在途批次数据` 10 个 Sheet，顺序不得改变。各站点 `净需求表` 的字段名行统一为第 2 行、数据从第 3 行开始；程序仍必须通过“类目目标”分组位置动态校验月份字段行，发现结构不一致时停止执行。
5. 映射表必须包含 `SKU信息表`、`店铺信息表`、`仓库信息表`。
6. 任一必需源文件缺失或无有效数据时停止执行，不生成半成品。

### 2.3 精确命名校验

`月度数据中间表` 必须包含以下 15 个 Sheet：

1. `A1类目销售数据表`
2. `A2类目SKU销售监控表`
3. `B1运营类目销售表`
4. `B2店铺类目销售表`
5. `A1类目销售数据口径`
6. `A2类目SKU销售监控数据口径`
7. `B1运营类目数据口径`
8. `B2店铺类目数据口径`
9. `千易商品销售统计`
10. `千易库存数据`
11. `千易入库单_收货中`
12. `SKU信息表`
13. `店铺信息表`
14. `仓库信息表`
15. `净需求-目标表`

`运营大盘数据监控表.xlsx` 必须包含以下 5 个 Sheet：

1. `产品动态分级表`
2. `A1类目销售数据表`
3. `A2类目SKU销售监控表`
4. `B1运营类目销售表`
5. `B2店铺类目销售表`

`在途批次表` 必须包含以下 12 个 Sheet，最终工作簿顺序为：

1. `千易入库单_新建`
2. `A未发数据表`
3. `入库单表`
4. `SKU批次表_泰国`
5. `SKU批次表_印尼1`
6. `SKU批次表_印尼2`
7. `SKU批次表_马来`
8. `SKU批次表_菲律宾`
9. `飞书-入库单跟进表`
10. `SKU信息表`
11. `店铺信息表`
12. `仓库信息表`

每个净需求模板及生成结果必须包含以下 10 个 Sheet：

1. `净需求表`
2. `计划目标表`
3. `货值情况`
4. `千易库存数据`
5. `千易入库单_收货中`
6. `销量占比`
7. `SKU信息表`
8. `仓库信息表`
9. `入库单信息`
10. `在途批次数据`

---

## 三、生成月度数据中间表

### 3.1 Sheet 生成顺序

按依赖顺序生成：

1. `A1类目销售数据口径`
2. `A2类目SKU销售监控数据口径`
3. `B1运营类目数据口径`
4. `B2店铺类目数据口径`
5. `SKU信息表`
6. `店铺信息表`
7. `仓库信息表`
8. `千易商品销售统计`
9. `千易入库单_收货中`
10. `千易库存数据`
11. `净需求-目标表`
12. `A1类目销售数据表`
13. `A2类目SKU销售监控表`
14. `B2店铺类目销售表`
15. `B1运营类目销售表`

### 3.2 三张映射表

| Sheet | 来源 | 操作 |
|---|---|---|
| `SKU信息表` | 映射表同名 Sheet | 原样复制全部字段；字段名使用当前版本的 `产品主类/材质`，不得使用旧名 `主类材质` |
| `店铺信息表` | 映射表同名 Sheet | 保留 `国家`、`店铺`、`运营`、`站点` |
| `仓库信息表` | 映射表同名 Sheet | 保留 `仓库代码`、`对应站点`、`站点缩写` |

### 3.3 `千易商品销售统计`

1. 从 `商品销售统计_M.xlsx / sheet1` 复制原始数据。
2. 原始表头可能被错误标记为仅 `A1`，必须先重置读取范围或流式扫描真实数据。
3. 保留原始字段，并新增以下字段：

| 字段 | 公式或逻辑 |
|---|---|
| `月份` | 从 `时间范围` 解析销售归属月，不加 1 个月；示例：`=TEXT(DATE(LEFT(AJ2,4),MID(AJ2,5,2),1),"yyyy年mm月")` |
| `季度` | 由 `月份` 换算，例如 `2026年第二季度` |
| `主SKU分类` | `系统sku` 匹配 `当前excel / SKU信息表 / 主SKU分类`；未匹配返回空白并记日志 |
| `运营` | `店铺` 匹配 `当前excel / 店铺信息表 / 运营`；未匹配返回空白 |
| `站点(中文)` | 按源 `站点` 到 `当前excel / 仓库信息表 / 站点缩写` 匹配 `对应站点` |
| `销售额VALUE` | `销售额 + 0`；无法转数值时返回 0 并记日志 |
| `销售sku数量VALUE` | `销售sku数量 + 0`；无法转数值时返回 0 并记日志 |
| `平均售价VALUE` | `平均售价 + 0`；无法转数值时返回 0 |

4. 现行列位参考：`销售额VALUE=R`、`销售sku数量VALUE=U`、`平均售价VALUE=AC`，但程序必须按字段名定位，不能硬编码列号。

### 3.4 `千易入库单_收货中`

1. 复制执行月1日的 `入库单明细_收货中_F.xlsx` 全部明细。例如 `M=2026-06` 时读取 `入库单明细_收货中_2026-06-01.xlsx`。
2. 现工作簿字段 `差异数据` 仅在批注文本包含“点完”时参与未收到库存计算，公式为：

```excel
=IF(ISNUMBER(FIND("点完",批注字段)),MAX(预计数量-仓库上架数量,0),0)
```

3. 当前 47 列结构下的样例公式为 `=IF(ISNUMBER(FIND("点完",AU2)),MAX(AC2-AF2,0),0)`，公式写入新增的 `AG` 列。程序仍必须按字段名定位 `批注`、`仓库上架数量`、`预计数量` 和 `差异数据` 对应列，禁止硬编码列号。减法方向固定为 `预计数量-仓库上架数量`。
4. 公式列使用浅蓝色背景。

### 3.5 `千易库存数据`

保留字段：`商品编码`、`商品名称`、`仓库`、`可用量`、`在途量`、`3天销量`、`7天销量`、`15天销量`、`30天销量`、`60天销量`、`90天销量`，并生成：

| 字段 | 公式或逻辑 |
|---|---|
| `月份` | 固定为 `DATA_MONTH_CN`，或绝对引用 `千易商品销售统计!$A$2` |
| `季度` | 固定为 `DATA_QUARTER_CN` |
| `主SKU分类` | `商品编码` 匹配 `SKU信息表 / 主SKU分类`；未匹配返回空白 |
| `站点（中文）` | `仓库` 匹配 `仓库信息表 / 对应站点` |
| `未收到库存` | 按 `商品编码 + 仓库` 汇总 `千易入库单_收货中 / 差异数据`；商品编码匹配 `商品编码`，库存仓库匹配入库单的 `收货仓` |
| `实际在途量` | `MAX(在途量-未收到库存,0)` |
| `库存合计` | `实际在途量 + 可用量` |

当前样例公式：

```excel
=SUMIFS('千易入库单_收货中'!AG:AG,'千易入库单_收货中'!P:P,C2)
=IF((I2-J2)<0,0,I2-J2)
=K2+H2
```

### 3.6 `净需求-目标表`

#### 3.6.1 数据来源与读取边界

唯一数据来源为：

`ROOT/数据中间表/净需求计算表/净需求计算表_{{站点_批次}}_DATA_MONTH_CN.xlsx / 净需求表`

1. 程序读取本次执行生成的 `菲律宾、马来、泰国、印尼1` 四份净需求结果文件，并且只读取每个工作簿中的 `净需求表` Sheet；缺少任一应生成文件时停止执行。`印尼2` 不进入月度数据中间表，文件存在与否均不读取。
2. 不读取、不解释也不复刻分站点净需求计算表的其他 Sheet 或内部计算链；本流程只判断 `净需求表` 中哪些字段进入月度中间表。
3. 各站点结果文件中 `净需求表` 的字段名行应统一为第 2 行、数据从第 3 行开始。程序仍需先定位“类目目标”分组，再校验其下一行的月份字段与执行月份窗口一致；发现字段行、月份行或数据起始行不一致时停止执行并输出文件与 Sheet。
4. 源 `净需求表` 中的目标、库存和批次字段可能本身是公式。月度中间表必须读取该工作簿最后一次由 Excel 保存的公式计算结果，并按值写入；不得把源公式、源工作簿外链或源 Sheet 内部引用复制进 `净需求-目标表`。
5. 每个站点文件读取完成后，按站点顺序纵向合并；不得跨站点去重。输出粒度为 `站点(CN) + 产品编码`。

#### 3.6.2 源字段到目标字段的取值关系

| 输出列 | 输出字段 | `净需求表` 来源 | 取值逻辑 |
|---|---|---|---|
| A | `站点(CN)` | 分站点结果文件名 `净需求计算表_{{站点_批次}}_DATA_MONTH_CN.xlsx` | 从文件名提取中文站点；`印尼1` 映射为 `印尼`，英文代码为 `ID`；不读取 `印尼2` |
| B | `站点(EN)` | 站点代码映射 | 根据 `站点(CN)` 映射站点英文代码，例如 `菲律宾→PH`、`马来→MY`、`泰国→TH`、`印尼→ID` |
| C | `主SKU分类` | B列 `主SKU分类` | 读取 Excel 已保存的计算结果值 |
| D | `材质` | C列 `材质` | 读取 Excel 已保存的计算结果值 |
| E | `子类目` | D列 `子类目` | 读取 Excel 已保存的计算结果值 |
| F | `菲律宾产品编码` | E列 `菲律宾产品编码` | 原值写入；其他站点如无该字段值则保留空白，不自行生成 |
| G | `产品编码` | F列 `产品编码` | 读取 Excel 已保存的计算结果值，作为 SKU 级匹配键 |
| H | `可售` | `库存情况` 分组下 L列 `可售` | 读取 Excel 已保存的计算结果值 |
| I | `实际在途` | `库存情况` 分组下 M列 `在途（待清点）` | 读取 Excel 已保存的计算结果值；输出字段统一命名为 `实际在途` |
| J:O | `类目目标YYYY.MM` | `类目目标` 分组下的月份字段 | 按执行月份 `M+1` 至 `M+6` 的月份文本 `YYYY.M月` 匹配后读取缓存值，输出表头使用 `YYYY.MM`；禁止仅按固定位置改名 |
| P:U | `SKU目标YYYY.MM` | `SKU目标` 分组下的月份字段 | 按执行月份 `M+1` 至 `M+6` 的月份文本 `YYYY.M月` 匹配后读取缓存值，输出表头使用 `YYYY.MM`；禁止仅按固定位置改名 |
| Z | `在途数量1` | AG列 `在途数量1` | 读取 Excel 已保存的计算结果值 |
| AA | `到货日期1` | AH列 `到货日期1` | 读取日期值并保留日期格式 |
| AB | `日期系数1` | AI列 `日期系数1` | 读取 Excel 已保存的计算结果值 |
| AC | `在途数量2` | AJ列 `在途数量2` | 读取 Excel 已保存的计算结果值 |
| AD | `到货日期2` | AK列 `到货日期2` | 读取日期值并保留日期格式 |
| AE | `日期系数2` | AL列 `日期系数2` | 读取 Excel 已保存的计算结果值 |
| AF | `在途数量3` | AM列 `在途数量3` | 读取 Excel 已保存的计算结果值 |
| AG | `到货日期3` | AN列 `到货日期3` | 读取日期值并保留日期格式 |
| AH | `日期系数3` | AO列 `日期系数3` | 读取 Excel 已保存的计算结果值 |
| AI | `在途数量4` | AP列 `在途数量4` | 读取 Excel 已保存的计算结果值 |
| AJ | `到货日期4` | AQ列 `到货日期4` | 读取日期值并保留日期格式 |
| AK | `日期系数4` | AR列 `日期系数4` | 读取 Excel 已保存的计算结果值 |
| AL | `类目目标PLAN_MONTH_DOT` | 分站点 `净需求表` 末尾辅助列 `类目目标YYYY.M月` | 读取执行月份 `M` 对应类目目标辅助列缓存值；该列专供 A1 `计划目标` 引用 |
| AM | `SKU目标PLAN_MONTH_DOT` | 分站点 `净需求表` 末尾辅助列 `SKU目标YYYY.M月` | 读取执行月份 `M` 对应 SKU 目标辅助列缓存值；该列专供 A2 `计划目标` 引用 |

`J:U` 必须先识别“类目目标”和“SKU目标”分组，再在其月份字段行按 `YYYY.M月` 文本匹配。主净需求月份窗口由净需求生成脚本按执行月份 `M` 动态生成 `M+1` 至 `M+6`；`AL:AM` 读取执行月份 `M` 的两个辅助目标列。任一月份或辅助列缺失、重复或错序，必须输出对应源列并停止生成，禁止仅按列位置强行改名。

#### 3.6.3 月度中间表新增公式字段

除 V:Y 外，其余字段均按上一节从源 `净需求表` 读取 Excel 缓存值。V:Y 是 `净需求-目标表` 内新增的公式列，公式从第 2 行填充到最后一个有效数据行。AL:AM 是从分站点净需求辅助列读取的缓存值，不在月度中间表内重新计算。

| 输出列 | 新增字段 | 引用字段 | 公式计算逻辑 | 第2行公式示例 |
|---|---|---|---|---|
| V | `SKU在途批次计数(月份限制)` | 本表 `站点(CN)`、四组 `在途数量/到货日期`；`千易商品销售统计 / 日期Value` | 以销售月份日期锚点为基准：菲律宾、马来、泰国累计 `M+1` 起至 `M+4` 前到货的四批在途数量，即 3 个月窗口；印尼累计 `M+1` 起至 `M+5` 前到货的四批在途数量，即 4 个月窗口。字段名虽为“批次计数”，当前实际累计的是满足窗口条件的在途数量，不统计批次数 | `=IFS(OR(A2="菲律宾",A2="马来",A2="泰国"),IF(AND(AA2>=EDATE(千易商品销售统计!$AK$2,1),AA2<EDATE(千易商品销售统计!$AK$2,4)),Z2,0)+IF(AND(AD2>=EDATE(千易商品销售统计!$AK$2,1),AD2<EDATE(千易商品销售统计!$AK$2,4)),AC2,0)+IF(AND(AG2>=EDATE(千易商品销售统计!$AK$2,1),AG2<EDATE(千易商品销售统计!$AK$2,4)),AF2,0)+IF(AND(AJ2>=EDATE(千易商品销售统计!$AK$2,1),AJ2<EDATE(千易商品销售统计!$AK$2,4)),AI2,0),A2="印尼",IF(AND(AA2>=EDATE(千易商品销售统计!$AK$2,1),AA2<EDATE(千易商品销售统计!$AK$2,5)),Z2,0)+IF(AND(AD2>=EDATE(千易商品销售统计!$AK$2,1),AD2<EDATE(千易商品销售统计!$AK$2,5)),AC2,0)+IF(AND(AG2>=EDATE(千易商品销售统计!$AK$2,1),AG2<EDATE(千易商品销售统计!$AK$2,5)),AF2,0)+IF(AND(AJ2>=EDATE(千易商品销售统计!$AK$2,1),AJ2<EDATE(千易商品销售统计!$AK$2,5)),AI2,0),TRUE,"")` |
| W | `SKU周转系数` | 本表 H列 `可售`、I列 `实际在途`、V列 `SKU在途批次计数(月份限制)` 及 P:U 的 `SKU目标YYYY.MM` | 菲律宾、马来、泰国：`(可售 + 实际在途 + SKU在途批次计数(月份限制)) / 前3个月SKU目标合计`；印尼：`(可售 + 实际在途 + SKU在途批次计数(月份限制)) / 前4个月SKU目标合计`；沿用当前错误返回文本 `计算错误` | `=IFERROR((H2+I2+V2)/IFS(OR(A2="菲律宾",A2="马来",A2="泰国"),P2+Q2+R2,A2="印尼",P2+Q2+R2+S2,TRUE,""),"计算错误")` |
| X | `类目在途批次计数` | 本表 A列 `站点(CN)`、C列 `主SKU分类`、H列 `可售`、I列 `实际在途`、V列 `SKU在途批次计数(月份限制)` | 按 `站点(CN) + 主SKU分类` 汇总同站点同类目下所有 SKU 的可售库存、实际在途和月份限制内的 SKU 在途数量；字段名虽为“批次计数”，当前实际为类目口径的库存与在途数量合计 | `=SUMIFS($H:$H,$A:$A,A2,$C:$C,C2)+SUMIFS($I:$I,$A:$A,A2,$C:$C,C2)+SUMIFS($V:$V,$A:$A,A2,$C:$C,C2)` |
| Y | `类目周转系数` | 本表 A列、C列、X列及 P:S 的 `SKU目标YYYY.MM` | 分母按 `站点(CN) + 主SKU分类` 汇总 SKU 目标：菲律宾、马来、泰国使用前3个月目标合计，印尼使用前4个月目标合计；结果为 `类目在途批次计数 / 同站点同类目目标合计`，无有效分母时返回空白 | `=IFERROR(X2/IFS(OR(A2="菲律宾",A2="马来",A2="泰国"),SUMIFS($P:$P,$A:$A,A2,$C:$C,C2)+SUMIFS($Q:$Q,$A:$A,A2,$C:$C,C2)+SUMIFS($R:$R,$A:$A,A2,$C:$C,C2),A2="印尼",SUMIFS($P:$P,$A:$A,A2,$C:$C,C2)+SUMIFS($Q:$Q,$A:$A,A2,$C:$C,C2)+SUMIFS($R:$R,$A:$A,A2,$C:$C,C2)+SUMIFS($S:$S,$A:$A,A2,$C:$C,C2),TRUE,"计算错误"),"")` |

当前列结构下可直接写入的第 2 行公式模板如下；实际执行时仍须按字段名定位列号：

```excel
=IFS(
  OR(A2="菲律宾",A2="马来",A2="泰国"),
  IF(AND(AA2>=EDATE('千易商品销售统计'!$AK$2,1),AA2<EDATE('千易商品销售统计'!$AK$2,4)),Z2,0)
  +IF(AND(AD2>=EDATE('千易商品销售统计'!$AK$2,1),AD2<EDATE('千易商品销售统计'!$AK$2,4)),AC2,0)
  +IF(AND(AG2>=EDATE('千易商品销售统计'!$AK$2,1),AG2<EDATE('千易商品销售统计'!$AK$2,4)),AF2,0)
  +IF(AND(AJ2>=EDATE('千易商品销售统计'!$AK$2,1),AJ2<EDATE('千易商品销售统计'!$AK$2,4)),AI2,0),
  A2="印尼",
  IF(AND(AA2>=EDATE('千易商品销售统计'!$AK$2,1),AA2<EDATE('千易商品销售统计'!$AK$2,5)),Z2,0)
  +IF(AND(AD2>=EDATE('千易商品销售统计'!$AK$2,1),AD2<EDATE('千易商品销售统计'!$AK$2,5)),AC2,0)
  +IF(AND(AG2>=EDATE('千易商品销售统计'!$AK$2,1),AG2<EDATE('千易商品销售统计'!$AK$2,5)),AF2,0)
  +IF(AND(AJ2>=EDATE('千易商品销售统计'!$AK$2,1),AJ2<EDATE('千易商品销售统计'!$AK$2,5)),AI2,0),
  TRUE,""
)
```

```excel
=IFERROR(V2/IFS(OR(A2="菲律宾",A2="马来",A2="泰国"),P2+Q2+R2,A2="印尼",P2+Q2+R2+S2,TRUE,""),"计算错误")
```

```excel
=SUMIFS($V:$V,$A:$A,A2,$C:$C,C2)
```

```excel
=IFERROR(X2/IFS(OR(A2="菲律宾",A2="马来",A2="泰国"),SUMIFS($P:$P,$A:$A,A2,$C:$C,C2)+SUMIFS($Q:$Q,$A:$A,A2,$C:$C,C2)+SUMIFS($R:$R,$A:$A,A2,$C:$C,C2),A2="印尼",SUMIFS($P:$P,$A:$A,A2,$C:$C,C2)+SUMIFS($Q:$Q,$A:$A,A2,$C:$C,C2)+SUMIFS($R:$R,$A:$A,A2,$C:$C,C2)+SUMIFS($S:$S,$A:$A,A2,$C:$C,C2),TRUE,"计算错误"),"")
```

公式实现要求：

1. `千易商品销售统计 / 日期Value` 必须按字段名定位，当前样例位于 `AK2`，不得长期硬编码列号。
2. Excel 文件内部引用统一使用带单引号的 Sheet 引用，例如 `'千易商品销售统计'!$AK$2`。
3. `SKU周转系数` 按 `站点(CN) + 产品编码` 使用；`类目周转系数` 按 `站点(CN) + 主SKU分类` 使用。
4. 仅 A1、A2取与 `M` 相同的 `PLAN_MONTH_DOT` 对应 `类目目标`、`SKU目标`。B1、B2 不读取 `PLAN_M` 或 `PLAN_MONTH_DOT`；B2 目标由结果表人工维护，B1 目标由结果表 B2 汇总。
5. 分站点净需求源工作簿允许存在 `#REF!` 和 `#DIV/0!`。程序只校验并读取进入 `净需求-目标表` 的指定字段，其他未引用区域的公式错误只记录日志，不阻断月度中间表生成。被读取字段中的 `#N/A` 必须原样写入，不得擅自改为空白、0或替代文本；其他错误值同样不得静默改写，必须记录来源位置。

### 3.7 四张数据口径表

四张数据口径表的唯一权威来源为：

`ROOT/源数据/映射表/@数据粒度表_运营大盘数据监控表.xlsx`

必须按照下表执行一对一取值，禁止使用任何其他工作簿或 Sheet 替代：

| 输出 Sheet | 来源工作簿 / 来源 Sheet | 取值字段 | 取值操作 |
|---|---|---|---|
| `A1类目销售数据口径` | `@数据粒度表_运营大盘数据监控表.xlsx / A1类目销售数据表（粒度）` | `供应商`、`站点（中文）`、`站点（英文）`、`产品分级`、`产品主类/材质`、`主SKU分类` | 按来源表字段顺序和有效数据行顺序逐行写入；不得从商品销售统计或现有 A1 主表推导，不得二次去重 |
| `A2类目SKU销售监控数据口径` | `@数据粒度表_运营大盘数据监控表.xlsx / A2类目SKU销售监控表（粒度）` | `供应商`、`站点（中文）`、`站点（英文）`、`产品分级`、`产品主类/材质`、`主SKU分类`、`产品编码`、`产品名称` | 按来源表字段顺序和有效数据行顺序逐行写入；不得使用现有 A2 主表、结果表或当月有销量 SKU 反推粒度 |
| `B1运营类目数据口径` | `@数据粒度表_运营大盘数据监控表.xlsx / B1运营类目销售表（粒度）` | `站点（中文）`、`站点（英文）`、`产品分级`、`产品主类/材质`、`类目`、`运营` | 按来源表字段顺序和有效数据行顺序逐行写入；后续生成 B1 主表时，仅将来源字段 `类目` 映射为主表字段 `主SKU分类` |
| `B2店铺类目数据口径` | `@数据粒度表_运营大盘数据监控表.xlsx / B2店铺类目销售数据（粒度） ` | `站点`、`店铺名`、`运营`、`产品分级`、`产品主类/材质`、`主SKU分类` | 按来源表字段顺序和有效数据行顺序逐行写入；来源 Sheet 名末尾实际存在一个空格，必须按实际名称精确读取并记录匹配日志 |

统一操作规则：

1. 每次生成月度中间表时，必须先读取 `@数据粒度表_运营大盘数据监控表.xlsx`，重新创建或全量覆盖四张数据口径表，禁止沿用上月缓存。
2. 从来源 Sheet 第 1 行读取表头，从第 2 行读取数据，截止到来源表最后一个有效数据行；按表中列出的字段逐列取值，不读取未列出的辅助字段。
3. 保留来源表的字段名、字段顺序、有效数据行顺序、文本值、数字值和空白值；粒度维度按单元格值写入，不复制外部链接，不新增公式列，不做额外映射，不自行补值。
4. 空行判定为整行所有取值字段均为空；仅排除来源有效数据区末尾的全空行，不删除有效数据区中的中间空行。
5. 不按业务粒度自行去重。若来源存在完全重复行或关键粒度重复，输出重复清单并停止生成主表，由源粒度表维护者处理。
6. 当前校准行数（不含表头）为 A1 `81` 行、A2 `4876` 行、B1 `406` 行、B2 `897` 行。行数只作为本次基准日志，不得硬编码为长期限制。
7. 写入完成后，逐张校验输出字段名、字段顺序、数据行数和逐行粒度值与来源 Sheet 一致；任一不一致均停止后续主表生成并输出差异位置。
8. 四张主数据表必须以对应口径表的完整行集合和顺序为基线，再逐行计算月度指标；不能只保留当月有销售、库存或目标的数据行。

### 3.8 `A1类目销售数据表`

粒度：`站点 + 主SKU分类`，每个粒度只能出现一行。

| 字段 | 来源或公式 |
|---|---|
| `供应商`、`站点`、`产品分级`、`产品主类/材质`、`主SKU分类` | 逐行取 `A1类目销售数据口径`；其中 `站点` 对应来源字段 `站点（中文）`，保持来源顺序 |
| `月份` | `DATA_MONTH_CN`，整列固定，不得序列填充 |
| `季度` | `DATA_QUARTER_CN` |
| `销量` | 按 `站点 + 主SKU分类` 汇总 `千易商品销售统计 / 销售sku数量VALUE` |
| `销售额` | 按 `站点 + 主SKU分类` 汇总 `千易商品销售统计 / 销售额VALUE` |
| `销售额占比` | `IFERROR(当前行销售额 / 同站点销售额合计,0)` |
| `计划目标` | 按 `站点 + 主SKU分类` 拼接匹配 `净需求-目标表 / 站点(CN) + 主SKU分类`，返回 `净需求-目标表 / 类目目标PLAN_MONTH_DOT` 辅助列（当前列结构为 AL）；生成公式使用 `XLOOKUP(站点&主SKU分类&"",净需求站点列&净需求主SKU分类列&"",净需求类目目标辅助列,"")`，未匹配时返回空白 |
| `实际库存` | 按 `站点 + 主SKU分类` 汇总 `千易库存数据 / 库存合计` |
| `备货准确率` | `IFERROR(实际库存 / 计划目标,"")` |
| `周转系数` | 按 `站点 + 主SKU分类` 匹配 `净需求-目标表 / 类目周转系数` |

关键公式必须使用错误保护；不得沿用会产生 `#VALUE!` 的 `=L2/K2`。

### 3.9 `A2类目SKU销售监控表`

粒度：`站点 + 产品编码`，每个粒度只能出现一行。完整行集合及排序以 `A2类目SKU销售监控数据口径` 为准，不得使用现有 A2 主表、结果表或当月有销量 SKU 反推。

| 字段 | 来源或公式 |
|---|---|
| `供应商`、`站点`、`产品分级`、`产品主类/材质`、`主SKU分类`、`产品编码`、`产品名称` | 逐行取 `A2类目SKU销售监控数据口径`；其中 `站点` 对应来源字段 `站点（中文）`，保持来源顺序 |
| `月份` | `DATA_MONTH_CN`，整列固定 |
| `季度` | `DATA_QUARTER_CN` |
| `计划目标` | 按 `站点 + 产品编码` 匹配 `净需求-目标表 / SKU目标PLAN_MONTH_DOT` 辅助列（当前列结构为 AM） |
| `实际销量` | 按 `站点 + 产品编码` 汇总 `销售sku数量VALUE` |
| `目标完成率` | `IFERROR(实际销量 / 计划目标,"")` |
| `实际库存` | 按 `站点 + 产品编码` 汇总 `千易库存数据 / 库存合计` |
| `备货准确率` | `IFERROR(实际库存 / 计划目标,"")` |
| `周转系数` | 按 `站点 + 产品编码` 匹配 `净需求-目标表 / SKU周转系数` |

### 3.10 `B2店铺类目销售表`

粒度：`站点 + 店铺名 + 主SKU分类`。

| 字段 | 来源或公式 |
|---|---|
| `站点`、`店铺名`、`运营`、`产品分级`、`产品主类/材质`、`主SKU分类` | 逐行取 `B2店铺类目数据口径`，保持来源顺序 |
| `月份` | `DATA_MONTH_CN`，与销售归属月一致，整列固定 |
| `季度` | `DATA_QUARTER_CN` |
| `实际库存` | 按 `站点 + 主SKU分类` 汇总 `千易库存数据 / 库存合计` |
| `实际销量` | 按 `站点 + 店铺 + 主SKU分类` 汇总销售归属月销量 |
| `客单价（RMB）` | 按相同粒度对 `平均售价VALUE` 做非零平均；无匹配时返回 0 |
| `实际销售额` | 按相同粒度汇总销售归属月 `销售额VALUE` |

`客单价（RMB）` 必须使用 `IFERROR(AVERAGEIFS(...),0)`，不得留下 `#DIV/0!`。

### 3.11 `B1运营类目销售表`

粒度：`站点 + 运营 + 主SKU分类`。

| 字段 | 来源或公式 |
|---|---|
| `站点`、`运营`、`产品分级`、`产品主类/材质`、`主SKU分类` | 逐行取 `B1运营类目数据口径`；`站点` 对应 `站点（中文）`，`主SKU分类` 对应来源字段 `类目`，保持来源顺序 |
| `月份` | `DATA_MONTH_CN`，整列固定 |
| `季度` | `DATA_QUARTER_CN` |
| `实际销量` | 按 `站点 + 运营 + 主SKU分类` 汇总销售归属月销量 |
| `实际销售额` | 按相同粒度汇总销售归属月销售额 |
| `销售额占比` | `IFERROR(当前行实际销售额 / 同站点同运营实际销售额合计,0)` |

### 3.12 保存、Excel 计算与缓存校验

1. 保存月度中间表，公式仍须为公式。
2. 调用 Microsoft Excel，设置全量计算，等待计算完成，保存并关闭。
3. 重新以公式模式和缓存值模式各读取一次。
4. 校验所有 15 个 Sheet、字段、行数、公式数量和公式缓存值。
5. 扫描 `#REF!`、`#N/A`、`#VALUE!`、`#DIV/0!`、`#NAME?`。允许 `净需求-目标表` 从分站点净需求源字段原样导入的 `#N/A`；除此之外的新公式错误均应停止后续流程。
6. 校验 A1/A2/B1/B2 月份均等于 `DATA_MONTH_CN`，且每个 Sheet 的月份列只能有一个非空月份值。

---

## 四、生成在途批次表

在途批次表继续作为独立中间表，禁止混入月度数据中间表。输出文件为：

`ROOT/数据中间表/在途批次表/在途批次表_DATA_MONTH_CN.xlsx`

在途批次表不依赖历史月份文件或已存在模板来生成。即使 `ROOT/数据中间表/` 中不存在任何历史 `在途批次表_YYYY年MM月.xlsx`，脚本也必须直接按本文档固定的 12 个 Sheet、字段顺序、公式逻辑、短日期格式、公式浅蓝填充、表头样式和最终 Sheet 顺序创建完整工作簿。禁止因为缺少历史模板而停止执行、改用其他月份文件或复制旧文件作为模板。

如果目标文件 `在途批次表_DATA_MONTH_CN.xlsx` 已经存在，执行前必须先清空本次会刷新的外部引用数据 Sheet、派生源 Sheet 和批次结果 Sheet 的旧数据行，再重新从源文件同步，防止重复追加或沿用旧批次结果。需要清空第 2 行起旧数据的 Sheet 包括：`千易入库单_新建`、`A未发数据表`、`飞书-入库单跟进表`、`SKU信息表`、`店铺信息表`、`仓库信息表`、`入库单表`、`SKU批次表_泰国`、`SKU批次表_印尼1`、`SKU批次表_印尼2`、`SKU批次表_马来`、`SKU批次表_菲律宾`。清空只针对旧数据区，不删除 Sheet，不破坏表头、列宽、公式列样式和工作簿结构；随后每个 Sheet 按本文档重新写入本次数据和公式。

其中 `M` 为脚本输入的销售归属月（格式 `YYYY-MM`），`BATCH_F` 为 `M` 的下一自然月 1 日。所有在途批次日快照源文件必须严格读取 `BATCH_F` 对应文件，不得检索、合并或回退到其他日期快照；A 未发数据表仍读取销售归属月 `M`。以 `M=2026-05` 为例，`BATCH_F=2026-06-01`：

| 源数据 | 严格路径 | 在本阶段的用途 |
|---|---|---|
| A未发数据表 | `ROOT/源数据/飞书人工维护数据/A未发数据表/A未发数据表_2026年05月.xlsx` | 生成 A 未发明细并参与批次计算 |
| 入库单跟踪表 | `ROOT/源数据/飞书人工维护数据/入库单跟踪表/入库单跟踪表_2026-06-01.xlsx` | 按入库单号提供发货、到仓及整合日期 |
| 库存查询 | `ROOT/源数据/千易库存数据/库存查询_2026-06-01.xlsx` | 作为次月月初库存快照进行存在性、表头和有效行校验；供后续“生成净需求计算表-分站点”使用，不直接参与批次划分 |
| 千易新建入库单 | `ROOT/源数据/千易入库单数据/入库单明细_新建_2026-06-01.xlsx` | 生成千易新建入库明细 |

任一精确日期文件缺失时立即停止并打印缺失路径。禁止遍历其他日期文件，禁止合并多个快照，也禁止自动改取月末、目录内最新文件或其他日期文件。

### 4.1 Sheet 生成顺序与最终排列顺序

程序必须按依赖顺序生成，不能直接按最终工作簿顺序计算：

1. `SKU信息表`
2. `店铺信息表`
3. `仓库信息表`
4. `飞书-入库单跟进表`
5. `A未发数据表`
6. `千易入库单_新建`
7. `入库单表`
8. `SKU批次表_泰国`
9. `SKU批次表_印尼1`
10. `SKU批次表_印尼2`
11. `SKU批次表_马来`
12. `SKU批次表_菲律宾`

全部 Sheet 生成后，再将工作簿标签调整为以下最终展示顺序：

1. `千易入库单_新建`
2. `A未发数据表`
3. `入库单表`
4. `SKU批次表_泰国`
5. `SKU批次表_印尼1`
6. `SKU批次表_印尼2`
7. `SKU批次表_马来`
8. `SKU批次表_菲律宾`
9. `飞书-入库单跟进表`
10. `SKU信息表`
11. `店铺信息表`
12. `仓库信息表`

每生成一个 Sheet，立即打印：Sheet 名、来源行数、输出行数、字段数、公式列、映射未命中数和公式错误数。

### 4.2 三张映射表

数据来源：`ROOT/源数据/映射表/SKU、店铺、仓库映射表.xlsx`。

| 输出 Sheet | 来源 Sheet | 数据操作 |
|---|---|---|
| `SKU信息表` | `SKU信息表` | 整表复制，保留原字段名、字段顺序、值、日期格式和数字格式；不得去重或改名 |
| `店铺信息表` | `店铺信息表` | 整表复制，核心字段为 `国家`、`店铺`、`运营`、`站点` |
| `仓库信息表` | `仓库信息表` | 整表复制，核心字段为 `仓库代码`、`对应站点`、`站点缩写` |

三张映射表必须先生成，因为 `千易入库单_新建` 的 `站点（中文）` 依赖 `仓库信息表`。映射表本身不新增公式列。

### 4.3 `飞书-入库单跟进表`

数据来源：`ROOT/源数据/飞书人工维护数据/入库单跟踪表/入库单跟踪表_BATCH_F.xlsx`。

数据操作：只读取精确的 `BATCH_F` 文件，不合并其他日期跟踪表。读取有效数据区并保留原始跟踪信息，以入库单号作为后续查询键。不得沿用样表中含 `#REF!` 的旧公式。

字段顺序固定为：

`仓库`、`站点`、`入库单号`、`发货时间`、`到仓时间`、`到仓时间整合`、`是否到货`、`到货天数`

| 字段 | 取值或公式逻辑 |
|---|---|
| `仓库` 至 `是否到货` | 从源文件同名字段读取；保留源值 |
| `到货天数` | `到仓时间-发货时间`；任一日期为空或计算异常时返回空白 |

`到货天数` 等价逻辑：

```excel
=IFERROR(E2-D2,"")
```

其中 D、E 仅为样例列位置，实际必须按字段名定位。`发货时间`、`到仓时间`、`到仓时间整合` 统一使用短日期格式 `yyyy-mm-dd`。

### 4.4 `千易入库单_新建`

数据来源：

- `ROOT/源数据/千易入库单数据/入库单明细_新建_BATCH_F.xlsx`
- 当前 Excel / `飞书-入库单跟进表`
- 当前 Excel / `仓库信息表`

数据操作：只读取 `入库单明细_新建_BATCH_F.xlsx`，保留指定 8 个字段；按 `收货仓` 映射中文站点，按 `入库单号` 回填同一 `BATCH_F` 日期的飞书跟踪表三个日期字段。不得汇总、去重或过滤合法明细行。A 未发数据保持在独立 Sheet，不在本 Sheet 重复追加。

字段顺序固定为：

`入库单号`、`收货仓`、`站点（中文）`、`商品编码`、`预计数量`、`发货时间`、`到仓时间`、`到仓时间整合`

| 字段 | 取值逻辑 |
|---|---|
| `入库单号`、`收货仓`、`商品编码`、`预计数量` | `入库单明细_新建_BATCH_F.xlsx` 同名字段 |
| `站点（中文）` | `收货仓` 精确匹配 `仓库信息表 / 仓库代码`，返回 `对应站点` |
| `发货时间` | `入库单号` 匹配 `飞书-入库单跟进表 / 发货时间` |
| `到仓时间` | `入库单号` 匹配 `飞书-入库单跟进表 / 到仓时间` |
| `到仓时间整合` | `入库单号` 匹配 `飞书-入库单跟进表 / 到仓时间整合` |

对应样表公式结构：

```excel
站点（中文） = VLOOKUP(收货仓,仓库信息表!仓库代码:站点缩写,2,0)
发货时间     = VLOOKUP(入库单号,'飞书-入库单跟进表'!入库单号:到货天数,2,0)
到仓时间     = VLOOKUP(入库单号,'飞书-入库单跟进表'!入库单号:到货天数,3,0)
到仓时间整合 = VLOOKUP(入库单号,'飞书-入库单跟进表'!入库单号:到货天数,4,0)
```

程序应使用实际表头定位列，不得硬编码列字母。映射未命中时输出入库单号清单；不得自动检索其他日期跟踪表补值。`发货时间`、`到仓时间`、`到仓时间整合` 统一使用短日期格式 `yyyy-mm-dd`。

### 4.5 `A未发数据表`

数据来源：`ROOT/源数据/飞书人工维护数据/A未发数据表/A未发数据表_DATA_MONTH_CN.xlsx` 中的 `印尼1`、`印尼2`、`菲律宾`、`马来`、`泰国` 五个站点 Sheet。

数据操作：按 `印尼1`、`印尼2`、`菲律宾`、`马来`、`泰国` 的固定顺序分别读取站点 Sheet，再将各 Sheet 中 SKU 非空的有效行按相同字段结构纵向拼接。源工作簿中的临时汇总 Sheet `未发数据` 不参与读取。五个站点 Sheet 必须存在；Sheet 存在但没有有效数据时允许按 0 行继续；任一 Sheet 缺失或缺少 `站点`、`仓库`、`SKU`、`数量`、`要求发货时间` 字段时停止执行并报告具体 Sheet 和缺少字段。拼接总行数必须等于五个站点 Sheet 有效 SKU 行数之和。

完成五个站点 Sheet 拼接后，先按站点清洗 SKU，再根据站点运输天数推算到仓时间，并将到仓日期归并到当月 8 日、18 日或 28 日。印尼站点继续使用仓库判断 `站点_批次` 为 `印尼1` 或 `印尼2`。A 未发数据保留在独立 Sheet，后续与千易新建明细共同合并到 `入库单表`，不得重复追加。

字段顺序固定为：

`站点`、`仓库`、`SKU`、`数量`、`要求发货时间`、`到仓时间`、`到仓时间整合`

| 字段 | 取值或公式逻辑 |
|---|---|
| `站点`、`仓库`、`数量`、`要求发货时间` | 从源文件五个站点 Sheet 的同名字段读取并纵向拼接；印尼仓库用于生成 `站点_批次` |
| `SKU` | 先读取源文件同名字段；当 `站点="菲律宾"` 且 SKU 以区分大小写的 `HSTEST-` 开头时，只删除开头的第一个 `HSTEST-`；其他站点或不符合前缀条件的 SKU 保持原值 |
| `到仓时间` | 菲律宾：`要求发货时间 + 50天`；马来：`+40天`；泰国：`+35天`；印尼：`+70天` |
| `到仓时间整合` | 到仓日为 1～10 日归并到当月 8 日；11～20 日归并到当月 18 日；21～月底归并到当月 28 日 |

等价公式逻辑：

```excel
SKU = IF(AND(站点="菲律宾",LEFT(SKU原值,7)="HSTEST-"),MID(SKU原值,8,LEN(SKU原值)-7),SKU原值)

到仓时间 = IFS(站点="菲律宾",要求发货时间+50,
                 站点="马来",要求发货时间+40,
                 站点="泰国",要求发货时间+35,
                 站点="印尼",要求发货时间+70)

到仓时间整合 = IFS(DAY(到仓时间)<=10,DATE(YEAR(到仓时间),MONTH(到仓时间),8),
                     DAY(到仓时间)<=20,DATE(YEAR(到仓时间),MONTH(到仓时间),18),
                     DAY(到仓时间)<=31,DATE(YEAR(到仓时间),MONTH(到仓时间),28))
```

SKU 清洗必须在日期计算和写入 `入库单表` 之前完成。只允许删除字符串开头的 `HSTEST-`，不得删除 SKU 中间或结尾出现的相同文本；删除后 SKU 为空、仅为空格或与原始站点规则不符时必须记录异常并停止。站点不属于泰国、印尼、马来、菲律宾时必须记录异常并停止，不得静默生成空日期。三个日期字段统一使用短日期格式 `yyyy-mm-dd`。

### 4.6 `入库单表`

数据来源：当前 Excel / `千易入库单_新建` 与当前 Excel / `A未发数据表`。

数据操作：先读取 `千易入库单_新建` 全部有效明细，再追加一次 `A未发数据表` 全部有效明细；保留仓库字段，生成 `站点_批次`，并新增数值字段 `预计数量Value`。A 未发数据只能在本步骤追加一次。

字段顺序固定为：

`入库单号`、`站点（中文）`、`仓库`、`站点_批次`、`商品编码`、`预计数量`、`预计数量Value`、`发货时间`、`到仓时间`、`到仓时间整合`

| 输出字段 | 取值逻辑 |
|---|---|
| `入库单号` | 千易明细取原入库单号；A 未发行固定为 `A未发` |
| `站点（中文）` | 千易明细取映射后的中文站点；A 未发行取 `站点` |
| `仓库` | 千易明细取 `收货仓`；A 未发行取 `仓库` |
| `站点_批次` | 非印尼站点等于 `站点（中文）`；印尼云仓1返回 `印尼1`，印尼云仓2返回 `印尼2`；无法识别的印尼仓库必须停止执行 |
| `商品编码` | 千易明细取 `商品编码`；A 未发行取清洗后的 SKU |
| `预计数量` | 千易明细取 `预计数量`；A 未发行取 `数量` |
| `预计数量Value` | `预计数量 + 0` |
| `发货时间` | `千易入库单_新建 / 发货时间` |
| `到仓时间` | `千易入库单_新建 / 到仓时间` |
| `到仓时间整合` | `千易入库单_新建 / 到仓时间整合` |

`预计数量Value` 用于把文本数量转换为可汇总数值。转换失败、数量为空或数量小于 0 时必须记录异常，不得直接进入批次汇总。
`发货时间`、`到仓时间`、`到仓时间整合` 统一使用短日期格式 `yyyy-mm-dd`。

### 4.7 五张 `SKU批次表` 的通用结构

数据来源：当前 Excel / `入库单表`。

每个站点批次 Sheet 一行对应一个 `商品编码`。行粒度严格来自 `入库单表`：先按当前 Sheet 对应的 `站点_批次` 筛选，再对 `商品编码` 去重，并保持其在 `入库单表` 中的首次出现顺序。不得使用 `SKU信息表` 的完整 SKU 清单补行，也不得写入其他 `站点_批次` 的 SKU。同一 SKU 最多横向展示前四个批次。

字段顺序固定为：

`商品编码`，以及第 1～4 批各 4 个字段：`第N批入库单号`、`第N批数量`、`第N批交货时间`、`第N批预计到货时间`。

合计 17 列。业务规则明确只保留前四批；第 5 批及以后不写入当前工作簿，但必须在日志中输出“超过四批的 SKU 数量和样例”，不作为执行错误。

对每个 `站点 + 商品编码` 执行以下逻辑：

1. 仅保留 `发货时间` 非空的 `入库单表` 行。
2. 按 `到仓时间整合` 从早到晚排序；相同预计到货时间下，再按 `发货时间` 从早到晚排序。
3. 按排序后的唯一 `发货时间` 划分批次；第 N 个唯一发货时间即第 N 批。
4. `第N批入库单号`：同站点、同 SKU、同发货时间的入库单号去重后，以 ` / ` 拼接。
5. `第N批数量`：同一批次的 `预计数量Value` 求和。
6. `第N批交货时间`：该批次对应的唯一 `发货时间`。
7. `第N批预计到货时间`：该批次内 `到仓时间整合` 的最小值。
8. 不足四批的剩余字段返回空白；如果某 SKU 存在于当前 `站点_批次` 的 `入库单表` 中，但全部记录的发货时间均为空，则保留该 SKU 行，四批字段均为空。

所有批次表的交货时间列和预计到货时间列统一使用短日期格式 `yyyy-mm-dd`，不得保留 `General` 格式或显示 Excel 日期序列号。

每个批次字段应保留 Excel 公式。公式必须使用实际末行构造引用范围，不能固定为 `$2:$10001`；所有公式外层使用空 SKU 判断和错误保护，但不能吞掉数据异常日志。

公式结构等价于：

```excel
有效条件 = (站点列=当前站点)*(商品编码列=当前SKU)*(发货时间列<>"")
排序发货时间 = UNIQUE(SORTBY(FILTER(发货时间列,有效条件),
                              FILTER(到仓时间整合列,有效条件),1,
                              FILTER(发货时间列,有效条件),1))
本批发货时间 = INDEX(排序发货时间,N)
本批条件 = 有效条件*(发货时间列=本批发货时间)

第N批入库单号 = TEXTJOIN(" / ",TRUE,UNIQUE(FILTER(入库单号列,本批条件)))
第N批数量 = SUM(FILTER(预计数量Value列,本批条件))
第N批交货时间 = 本批发货时间
第N批预计到货时间 = MIN(FILTER(到仓时间整合列,本批条件))
```

### 4.8 五张 `SKU批次表` 的站点参数与生成顺序

| 生成顺序 | 输出 Sheet | `入库单表 / 站点_批次`筛选值 | 行粒度 |
|---|---|---|---|
| 1 | `SKU批次表_泰国` | `泰国` | `入库单表`中泰国的唯一 `商品编码`，按首次出现顺序 |
| 2 | `SKU批次表_印尼1` | `印尼1` | `入库单表`中印尼1仓的唯一 `商品编码`，按首次出现顺序 |
| 3 | `SKU批次表_印尼2` | `印尼2` | `入库单表`中印尼2仓的唯一 `商品编码`，按首次出现顺序 |
| 4 | `SKU批次表_马来` | `马来` | `入库单表`中马来的唯一 `商品编码`，按首次出现顺序 |
| 5 | `SKU批次表_菲律宾` | `菲律宾` | `入库单表`中菲律宾的唯一 `商品编码`，按首次出现顺序 |

五张表必须逐张生成、逐张打印日志，不能并行写入同一工作簿。每张表完成后校验：SKU 行数、SKU 集合及行顺序均与 `入库单表` 对应 `站点_批次` 去重结果完全一致，SKU 唯一、站点无串表、数量为数值、批次时间非递减、同批入库单号无重复。

### 4.9 保存、Excel 计算与缓存校验

1. 先保存一次在途批次表，所有新增公式仍须为 Excel 公式。
2. 调用 Microsoft Excel 全量计算，等待计算完成，保存并关闭。
3. 以公式模式和缓存值模式重新读取工作簿。
4. 校验 12 个 Sheet 名称、最终顺序、字段名、字段顺序、数据行数、公式数量和缓存值。
5. 扫描 `#REF!`、`#N/A`、`#VALUE!`、`#DIV/0!`、`#NAME?`、`#NUM!`；任一存在则执行失败。
6. 校验 `千易入库单_新建` 行数等于千易新建源文件有效行数，不得包含 A 未发行。
7. 校验 `A未发数据表` 有效行数等于源文件 `印尼1`、`印尼2`、`菲律宾`、`马来`、`泰国` 五个站点 Sheet 的有效 SKU 行数之和；确认未读取 `未发数据` Sheet。校验 `入库单表` 有效行数等于 `千易入库单_新建` 与 `A未发数据表` 有效行数之和；A 未发数据不得重复追加。
8. 校验菲律宾 A 未发行写入 `A未发数据表 / SKU` 和 `入库单表 / 商品编码` 后均不再以 `HSTEST-` 开头；非菲律宾和原本不以该前缀开头的 SKU 必须保持原值。
9. 校验每张批次表的 SKU 行数、集合和排序均等于 `入库单表` 对应 `站点_批次` 下 `商品编码` 的去重非空结果；不得从 `SKU信息表` 补充额外 SKU 行。
10. 校验每个批次数量等于对应站点、SKU、发货时间下 `预计数量Value` 的合计。
11. 如果目标文件已存在，校验本次运行后 `千易入库单_新建`、`A未发数据表`、`飞书-入库单跟进表`、`入库单表` 以及五张 `SKU批次表_*` 的有效行数只等于本次源数据生成结果，不得包含上次运行残留行。
12. 输出在途批次表保存路径、Excel 计算耗时、运行前清空的 Sheet 和旧行数、五个 A 未发源 Sheet 各自有效行数及拼接总行数、A 未发追加行数、菲律宾 SKU 去前缀行数、公式错误数、超过四批 SKU 数和最终校验结果。

---

## 五、生成净需求计算表-分站点

### 5.1 适用范围与输出文件

以 `@净需求计算表-菲律宾.xlsx` 为公式和版式模板。马来、泰国、印尼等站点使用相同的 Sheet 结构和计算流程，只替换站点参数、站点批次 Sheet、站点 SKU 字段、运输时间和币价字段。

模板路径：`ROOT/源数据/净需求计算表/@净需求计算表-{{站点模板名}}.xlsx`。

输出文件：`ROOT/数据中间表/净需求计算表/净需求计算表_{{站点_批次}}_DATA_MONTH_CN.xlsx`。

每个模板及生成结果包含以下 10 个 Sheet，名称和顺序必须保持一致：

1. `净需求表`
2. `计划目标表`
3. `货值情况`
4. `千易库存数据`
5. `千易入库单_收货中`
6. `销量占比`
7. `SKU信息表`
8. `仓库信息表`
9. `入库单信息`
10. `在途批次数据`

执行依赖顺序必须为：

1. 先生成 `在途批次表_DATA_MONTH_CN.xlsx`。
2. 复制现有分站点净需求工作簿作为模板，保留人工目标、历史销量窗口、粒度行、样式和行级包装取整规则。
3. 依次刷新 `SKU信息表`、`仓库信息表`、`千易入库单_收货中`、`千易库存数据`、`入库单信息`、`在途批次数据`。
4. 保留 `销量占比` 的历史窗口和公式，不从空白重建。
5. 保留并刷新 `计划目标表` 的公式字段，不覆盖人工填写的产品分级、月度目标和实际完成值。
6. 按原粒度重写 `净需求表` 公式。
7. 最后刷新 `货值情况` 汇总公式。
8. 调用 Microsoft Excel 全量计算、保存并关闭，再读取缓存值供月度数据中间表的 `净需求-目标表` 使用。

不得从空白工作簿直接重建分站点净需求表。当前未提供 `计划目标表` 的管理目标来源以及 `销量占比` 的完整历史快照来源，因此必须以现有站点工作簿为模板保留这些人工和历史输入。

### 5.2 外部数据 Sheet

#### 5.2.1 `SKU信息表`（产品信息表）

数据来源：

`ROOT/源数据/映射表/SKU、店铺、仓库映射表.xlsx / SKU信息表`

数据操作：整表按值复制到当前 Excel 的 `SKU信息表`，保留全部 46 个字段、字段顺序、空白值、文本 SKU、数值价格和备注。该 Sheet 是模板中的产品信息表，公式统一引用实际名称 `SKU信息表`，不得再引用不存在的 `产品信息表`。

菲律宾净需求表主要使用以下字段：

| 字段 | 用途 |
|---|---|
| `供应商代码` | 返回净需求行供应商；决定部分发货周期 |
| `材质`、`子类目`、`主SKU分类` | 补充产品维度 |
| `SKU` | 站点内部产品编码匹配键 |
| `菲律宾商品编码SKU` | 菲律宾平台商品编码 |
| `产品名称/颜色` | 产品名称、颜色和尺码解析来源 |
| `出厂成本价-不含海运费(YYYY.M)` | 出厂价及货值计算 |
| `PH成本价+海运费` | 菲律宾含海运成本及货值计算 |

其他站点需按对应站点价格字段替换菲律宾价格字段，但公式结构保持一致。

#### 5.2.2 `仓库信息表`与`千易入库单_收货中`

`仓库信息表`来源于 `ROOT/源数据/映射表/SKU、店铺、仓库映射表.xlsx / 仓库信息表`，按值整表复制，用于仓库到站点映射。

`千易入库单_收货中`来源于 `ROOT/源数据/千易入库单数据/入库单明细_收货中_BATCH_F.xlsx`。先按 `收货仓`筛选当前 `站点_批次`，再写入 48 列：源 A:G、公式列 `站点`、源 H:AF、公式列 `差异数据`、源 AG:AT。公式列保留浅蓝色背景：

- `站点`：`收货仓`匹配当前工作簿 `仓库信息表 / 对应站点`。
- `差异数据`：批注包含“点完”时返回 `MAX(预计数量-仓库上架数量,0)`，否则返回 0。公式由 Python 按字段名生成，不沿用站点模板中的旧差异公式。

#### 5.2.3 `千易库存数据`

数据来源：

`ROOT/源数据/千易库存数据/库存查询_BATCH_F.xlsx`

不得直接复制旧净需求工作簿中的库存缓存。按当前 `站点_批次`筛选月初快照并写入以下 16 个字段：

`商品编码`、`商品名称`、`主SKU分类`、`仓库`、`站点（中文）`、`可用量`、`在途量`、`未收到库存`、`实际在途量`、`库存合计`、`3天销量`、`7天销量`、`15天销量`、`30天销量`、`60天销量`、`90天销量`。

| 处理步骤 | 逻辑 |
|---|---|
| SKU 映射 | `商品编码` 匹配当前 Excel `SKU信息表 / SKU`，返回 `主SKU分类` |
| 站点映射 | `仓库` 匹配仓库映射表，返回 `站点（中文）` |
| 未收到库存 | 按 `商品编码+仓库` 汇总当前工作簿 `千易入库单_收货中 / 差异数据` |
| 实际在途量 | `MAX(在途量-未收到库存,0)` |
| 库存合计 | `可用量+实际在途量` |

分站点工作簿只保留当前 `站点_批次`库存行。印尼1、印尼2必须按仓库分别筛选，不得仅按“印尼”站点合并。

#### 5.2.4 `入库单信息`

数据来源：

`ROOT/数据中间表/在途批次表/在途批次表_DATA_MONTH_CN.xlsx / 入库单表`

每次执行先清空 Sheet 第 2 行起全部旧数据，再按 `站点_批次={{站点_批次}}` 筛选并投影为 6 列。只复制 Excel 计算后的值，不复制中间表公式。

| 输出字段 | `入库单表` 来源字段 | 取值逻辑 |
|---|---|---|
| `入库单号` | `入库单号` | 原值写入，包括固定文本 `A未发` |
| `仓库` | `仓库` | 原值写入；位于商品编码列前，用于保留站点批次的仓库证据 |
| `商品编码` | `商品编码` | 原值写入，菲律宾 A 未发行应已删除 `HSTEST-` 前缀 |
| `预计数量` | `预计数量Value` | 使用可汇总数值，不使用原始文本数量 |
| `交货时间` | `发货时间` | 读取 Excel 缓存日期 |
| `到货时间` | `到仓时间整合` | 直接读取在途批次表计算后的统一到仓日期 |

当前菲律宾模板部分 `到货时间` 使用 `交货时间+35`。新生成逻辑以 `入库单表 / 到仓时间整合` 为权威值，不再在净需求工作簿内重复估算运输天数。

#### 5.2.5 `在途批次数据`

数据来源：

`ROOT/数据中间表/在途批次表/在途批次表_DATA_MONTH_CN.xlsx / SKU批次表_{{站点_批次}}`

菲律宾、马来、泰国分别读取同名站点 Sheet；印尼1读取 `SKU批次表_印尼1`，印尼2读取 `SKU批次表_印尼2`。整表按值复制 17 列：

`商品编码`，以及第 1～4 批的 `入库单号`、`数量`、`交货时间`、`预计到货时间`。

目标 Sheet 的字段名、字段顺序、SKU 行顺序必须与来源 Sheet 完全一致。不得在净需求工作簿内重新划分批次、重新汇总数量或重新计算预计到货时间。

### 5.3 `销量占比`

行粒度为 `产品编码`。当前工作簿中的历史销量窗口属于模板历史数据，必须保留；当期字段可由 `千易库存数据` 和 `SKU信息表` 刷新。

| 字段组 | 来源或公式逻辑 |
|---|---|
| `主SKU分类`、`产品编码`、`产品名称`、`尺码` | 由模板粒度或 `SKU信息表` 按产品编码匹配 |
| `颜色` | 从产品名称提取颜色代码；当前模板示例为 `MID(产品名称,FIND("N",产品名称),4)`，无法识别时保留模板值并输出异常 |
| `可售`、`在途` | 按当前站点和产品编码读取 `千易库存数据 / 可用量、实际在途量` |
| `7/15/30/60天内出库数` | 按当前站点和产品编码读取 `千易库存数据 / 7/15/30/60天销量` |
| 历史 `90天内出库数` 窗口 | 保留模板中 X:AH 的历史值和月份标识；如新增当月快照，应在最右侧追加后维持固定窗口长度 |
| `最大值` | `MAX(历史90天销量窗口)` |
| `SKU占比` | `最大值 / 同主SKU分类最大值合计` |
| `颜色总出库数` | 按 `主SKU分类+颜色` 汇总最大值 |
| `SKU总出库数` | 按 `主SKU分类` 汇总最大值 |
| `颜色销量占比` | `IFERROR(颜色总出库数/SKU总出库数,0)` |
| `尺码总出库数` | 按 `主SKU分类+尺码` 汇总最大值 |
| `尺码销量占比` | `IFERROR(尺码总出库数/SKU总出库数,0)` |
| `新品/老品` | 保留模板管理值；当前模板通过产品编码回查 `净需求表 / 新品/老品` |

代表公式：

```excel
最大值       = MAX(历史90天销量窗口)
颜色总出库数 = SUMIFS(最大值列,主SKU分类列,当前主SKU分类,颜色列,当前颜色)
SKU总出库数  = SUMIFS(最大值列,主SKU分类列,当前主SKU分类)
颜色销量占比 = IFERROR(颜色总出库数/SKU总出库数,0)
尺码总出库数 = SUMIFS(最大值列,主SKU分类列,当前主SKU分类,尺码列,当前尺码)
尺码销量占比 = IFERROR(尺码总出库数/SKU总出库数,0)
```

### 5.4 `计划目标表`

该 Sheet 同时包含管理层人工输入和公式字段。必须以现有站点模板为基础更新，禁止清空后重建。

| 字段组 | 处理方式 |
|---|---|
| `产品分级`、`主SKU分类` | 保留模板粒度、排序和人工值 |
| 各月 `目标` | 管理层人工输入，原值保留；新增月份必须由用户或管理层提供 |
| 各月 `实际完成` | 已发生月份保留模板实际值；未来月份保持原状态，不擅自生成 |
| 各月 `完成率` | `IFERROR(实际完成/目标,"")` |
| `供应商` | `主SKU分类` 匹配 `SKU信息表 / 供应商代码` |
| `可售（在库）` | 按主SKU分类汇总 `净需求表 / 可售` |
| `在途` | 按主SKU分类汇总 `净需求表 / 在途` |
| `供应商未发数` | 按主SKU分类汇总 `净需求表 / 未发` |
| `全部合计` | `可售（在库）+在途+供应商未发数` |
| `30天内出库数` | 当前工作簿已按站点批次过滤，按主SKU分类汇总 `千易库存数据 / 30天销量` |
| `90天内出库数` | 当前工作簿已按站点批次过滤，按主SKU分类汇总 `千易库存数据 / 90天销量` |

当前分站点工作簿的 `千易库存数据` 只包含本 `站点_批次`，代表公式为：

```excel
=IFERROR(SUMIFS('千易库存数据'!$P:$P,'千易库存数据'!$C:$C,当前主SKU分类),0)
```

### 5.5 `净需求表`

#### 5.5.1 表头与粒度

第 1 行为来源说明，第 2 行为字段分组，第 3 行为字段名，第 4 行起为数据。当前菲律宾模板为 A:EX 共 154 列。

行粒度为 `产品编码`。现有模板中的产品编码集合、排序、`新品/老品` 和每个 SKU 的包装取整单位必须保留。当前资料无法证明该粒度完全等于 `SKU信息表` 某一个站点标记字段，因此不得仅凭“有备货”重新筛选行集合。

基础维度字段按产品编码从 `SKU信息表` 刷新：

| 字段 | 来源 |
|---|---|
| `供应商` | `SKU信息表 / 供应商代码` |
| `主SKU分类` | `SKU信息表 / 主SKU分类` |
| `材质` | `SKU信息表 / 材质` |
| `子类目` | `SKU信息表 / 子类目` |
| `菲律宾产品编码` | `SKU信息表 / 菲律宾商品编码SKU` |
| `产品编码` | `SKU信息表 / SKU` |
| `颜色/尺码` | `SKU信息表 / 产品名称/颜色` |
| `单价` | 菲律宾取 `SKU信息表 / 出厂成本价-不含海运费` |
| `新品/老品` | 保留模板管理值 |

#### 5.5.2 库存、目标和在途批次

| 字段组 | 核心公式逻辑 |
|---|---|
| `花色销量占比` | 产品编码匹配 `销量占比 / 颜色销量占比` |
| `尺寸销量占比` | 产品编码匹配 `销量占比 / 尺码销量占比` |
| `可售` | 按当前站点和产品编码读取 `千易库存数据 / 可用量` |
| `在途（待清点）` | 按当前站点和产品编码读取 `千易库存数据 / 实际在途量` |
| `在途` | `IFERROR(SUMPRODUCT(产品编码精确匹配入库单信息/商品编码，且入库单号<>"A未发"，汇总入库单信息/预计数量),0)+在途（待清点）`；其中商品编码匹配需使用 `TRIM(CLEAN())` 清洗并用 `EXACT()` 精确匹配 |
| `总在途` | `在途（待清点）+第1～4批在途数量` |
| `总库存` | `可售+总在途` |
| 六个月 `类目目标` | 主SKU分类匹配 `计划目标表` 对应月份目标 |
| 六个月 `SKU目标` | `类目目标×花色销量占比×尺寸销量占比` |
| 第1～4批 `在途数量/到货日期` | 产品编码匹配 `在途批次数据` 对应批次字段 |
| 第1～4批 `日期系数` | `1-DAY(到货日期)/30`；无到货日期时按模板返回值处理 |
| `周转系数` | `(可售+实际在途+指定窗口内批次数量)/指定月份SKU目标合计`，除零返回 0 |

##### 动态月份窗口与计划目标引用

1. `净需求表` 的主目标月份窗口以执行月份 `M` 的下一月为起始月份，连续覆盖 `M+1` 至 `M+6` 共 6 个月。示例：执行月份为 `2026-06` 时，主窗口为 `2026.7月` 至 `2026.12月`；执行月份为 `2026-07` 时，主窗口为 `2026.8月` 至 `2027.1月`。
2. 月份字段统一使用 `YYYY.M月` 格式，不补前导零。库存字段统一使用 `YYYY.M月库存` 格式。
3. 程序必须先读取 `计划目标表` 第 1 行，按 `YY年M月目标` 或 `YYYY年M月目标` 解析真实目标月份列，再生成公式。禁止长期固定引用 `AN`、`AQ` 等列号。
4. 执行前必须检查 `计划目标表` 是否同时存在执行月份 `M` 与主窗口 `M+1` 至 `M+6` 七个目标月份。其中 `M` 只用于 `净需求表` 末尾两列辅助目标，`M+1` 至 `M+6` 用于主净需求窗口。若目标月份缺失，必须在控制台打印缺失的站点模板和缺失月份，对应目标列按 `0` 写入并继续执行；若同一目标月份重复，则无法判断真实来源，必须停止执行。
5. `计划目标表` 由人工持续维护，本流程只读取目标月份和目标值，不新增月份列，不覆盖人工目标。

受影响的 Sheet 和字段如下：

| Sheet | 字段或列区间 | 影响方式 |
|---|---|---|
| `计划目标表` | `M`、`M+1` 至 `M+6` 对应的七个月目标列 | 作为公式引用来源；只校验和读取，不修改；缺少月份时记录日志，并让对应目标值按 `0` 写入 |
| `净需求表` | `P:U / 类目目标` | 表头按 `M+1` 至 `M+6` 更新；每列公式按目标月份重新定位 `计划目标表` 的真实目标列；若该月目标列缺失，整列写入 `0` |
| `净需求表` | `V:AA / SKU目标` | 表头按 `M+1` 至 `M+6` 更新；公式继续引用同位置的类目目标及销量占比 |
| `净需求表` | `AC:AF / 月度预计库存` | 表头按 `M` 至 `M+3` 更新为 `YYYY.M月库存`；库存承接公式保持原相对月份顺序 |
| `净需求表` | `BM:BR / 目标金额（出厂价）` | 表头按 `M+1` 至 `M+6` 更新；通过 SKU 目标间接受月份窗口影响 |
| `净需求表` | `BS:BX / 目标金额（出厂+海运）` | 同步更新月份表头；公式结构保持不变 |
| `净需求表` | `BY:CD / 目标金额（销售额）` | 同步更新月份表头；公式结构保持不变 |
| `净需求表` | `CE:CJ / 目标金额（预估账单收入）` | 同步更新月份表头；公式结构保持不变 |
| `净需求表` | `CK:CP / 库存金额（出厂价）` | 同步更新月份表头；公式继续引用对应月份预计库存 |
| `净需求表` | `CQ:CV / 库存金额（出厂+海运）` | 同步更新月份表头；公式结构保持不变 |
| `净需求表` | `CW:DB / 库存金额（预估销售额）` | 同步更新月份表头；公式结构保持不变 |
| `净需求表` | `DC:DH / 库存金额（预估账单收入）` | 同步更新月份表头；公式结构保持不变 |
| `净需求表` | `DI:DN / 月周转率（预计库存/目标）` | 同步更新月份表头；分子、分母继续使用同一月份的库存和目标 |
| `净需求表` | 末尾辅助列 `类目目标YYYY.M月`、`SKU目标YYYY.M月` | 每次执行前先清理历史辅助目标列，只保留本次执行月份 `M` 的一组辅助列；该月份也是主窗口首月 `M+1` 的前一月。`类目目标` 按主SKU分类引用 `计划目标表` 真实目标列，若执行月目标列缺失则写入 `0`；`SKU目标` 为该类目目标乘花色销量占比和尺寸销量占比；供月度中间表 A1/A2 计划目标引用 |

`净需求表 / P:U` 与末尾两列辅助目标直接引用 `计划目标表`。末尾辅助目标列不得历史堆叠；例如执行 `2026-06` 时，只允许保留 `类目目标2026.6月` 与 `SKU目标2026.6月`，不得同时保留 `2026.5月` 等旧辅助列。其余月份字段通过 `P:U`、`V:AA` 和 `AC:AF` 的既有计算链间接受影响，不得因为移动月份窗口而改变原公式结构。

库存匹配不得使用当前模板中只按产品编码的 `VLOOKUP`。分站点 Sheet 仅保留当前站点库存时可按产品编码匹配；若库存 Sheet 保留多站点，则必须使用 `SUMIFS` 同时限制站点和产品编码。

#### 5.5.3 预计库存与备货需求

| 字段组 | 公式逻辑 |
|---|---|
| 月度预计库存 | 按月承接上月剩余库存，加入当月到货批次数量，再扣减当月 SKU 目标；结果使用 `MAX(...,0)`，不得为负数 |
| 第一批净需求量 | 库存足以覆盖目标窗口时为 0；不足时根据周转系数和未来目标计算缺口，再乘 `1.2` 安全系数 |
| 第一批本次下单时间 | 模板人工日期或本次执行参数日期，整列不得序列填充 |
| 第一批发货时间 | 供应商C：下单时间 `+45天`；其他供应商：`+28天` |
| 第一批上架时间 | `发货时间+35天` |
| 第一批销售目标日期 | `上架时间+19天` |
| 第一批到货月份占比系数 | `1-DAY(上架时间)/30` |
| 第一批销售月份占比系数 | `DAY(销售目标日期)/30` |
| 第一批需求发货数 | 净需求量按 SKU 对应包装单位取整；无需求返回 0 |
| 第二批净需求量 | 在第一批需求量基础上重新计算下一目标窗口缺口，并扣除第一批需求发货数 |
| 第二批发货时间 | 供应商C：下单时间 `+60天`；其他供应商：`+48天` |
| 第二批上架时间 | `发货时间+35天` |
| 第二批销售目标日期 | `上架时间+15天` |
| 第二批需求下单数 | 第二批净需求量按 SKU 对应包装单位取整；无需求返回 0 |

包装取整单位不是全表统一常量。菲律宾样本同时存在按 10、50、100、120 等单位取整的行，必须逐行保留模板中的取整公式或维护独立包装单位字段，不得把整列统一改成 120。

模板中的月份表头、预计库存区间、下单时间及到货判断日期目前包含具体年月日。生成时必须根据 `M`、目标月份和执行参数动态替换，禁止长期保留 `DATE(2026,...)` 或固定的 `2026-06-10` 等样例日期。

#### 5.5.4 金额与周转指标

| 字段组 | 公式逻辑 |
|---|---|
| `出厂价` | 产品编码匹配 `SKU信息表 / 出厂成本价-不含海运费` |
| `出厂价+海运费` | 菲律宾匹配 `SKU信息表 / PH成本价+海运费` |
| 目标金额（出厂价） | `SKU月目标×出厂价` |
| 目标金额（出厂+海运） | `SKU月目标×（出厂价+海运费）` |
| 目标金额（预估账单收入） | `目标金额（出厂+海运）×1.25` |
| 目标金额（销售额） | `目标金额（预估账单收入）×1.3` |
| 库存数量 | 分为 `可售`、`在途`、`未发` 和三者合计 |
| 库存金额（出厂价） | 各库存数量分别乘出厂价，再求和 |
| 库存金额（出厂+海运） | 各库存数量分别乘含海运成本，再求和 |
| 库存金额（预估账单收入） | 对应含海运金额 `×1.25` |
| 库存金额（预估销售额） | 对应预估账单收入 `×1.3` |
| 月周转率 | 对应月预计库存数量除以对应月 SKU 目标 |
| 全盘周转率 | 未来三个月库存预估销售额合计除以未来三个月目标销售额合计，除零返回 0 |
| 第一/第二批备货金额 | 需求数量分别乘出厂价、含海运成本，并依次乘 `1.25`、`1.3` 得到账单收入和销售额 |
| 到货后周转率 | `(当前库存预估销售额+已规划批次销售额)/对应目标销售额窗口`，除零返回 0 |

### 5.6 `货值情况`

该 Sheet 按 `主SKU分类` 汇总 `净需求表`，用于管理层查看站点库存、目标和两批备货货值。第 4 行为合计行，第 5 行起为类目行。

| 字段组 | 计算逻辑 |
|---|---|
| `供应商` | 主SKU分类匹配 `净需求表 / 供应商` |
| `产品分级`、`主SKU分类` | 保留模板粒度与排序 |
| 数量：`可售/在途/未发/总数量` | 按主SKU分类分别汇总 `净需求表` 对应库存数量 |
| 出厂价成本 | 汇总 `净需求表` 出厂价库存金额 |
| 出厂+海运成本 | 汇总 `净需求表` 含海运库存金额 |
| 预估账单收入 | `出厂+海运成本×1.25` |
| 预估销售额 | `预估账单收入×1.3` |
| `目标销量` | 汇总未来三个月 SKU 目标 |
| `目标账单收入` | 汇总未来三个月目标账单收入 |
| `目标销售额` | 汇总未来三个月目标销售额 |
| `全盘周转率` | `IFERROR((可售+在途)/目标销量,0)` |
| 第一批、第二批备货数据 | 分别汇总净需求数量、出厂价金额、含海运金额、预估账单收入、预估销售额，并取对应到货时间和销售目标时间 |
| 批次到货后周转率 | `(批次数量+当前总库存)/目标销量`，除零返回 0 |

合计行对每个数值列使用 `SUM(类目数据区)`；日期列不求和。类目行数变化时，合计公式范围必须动态覆盖全部有效类目行。

### 5.7 Excel 计算、保存与校验

1. 10 个 Sheet 必须全部存在，名称和顺序正确。
2. `SKU信息表`、`仓库信息表`、`千易入库单_收货中`、`千易库存数据`、`入库单信息`、`在途批次数据` 的实际来源路径、源 Sheet、输入行数和输出行数必须打印日志。
3. `入库单信息` 只包含当前站点；`在途批次数据` 必须与 `SKU批次表_{{站点}}` 的字段、行数和数值一致。
4. `千易库存数据` 只包含当前 `站点_批次`；印尼1和印尼2的仓库集合不得交叉。
5. `净需求表` 产品编码不得重复，字段 A:EX 保持 154 列；公式填充至最后一个有效产品行。
6. `计划目标表` 的人工目标、实际完成值和产品分级不得因刷新为空而被覆盖。
7. `销量占比` 的历史 90 天销量窗口不得丢失；颜色和尺码占比分母为 0 时返回 0。
8. `货值情况` 类目汇总与 `净需求表` 明细汇总必须一致。
9. 保存后调用 Microsoft Excel 执行全量计算，等待计算完成，再次保存并关闭。
10. 以公式模式和缓存值模式重新读取，扫描 `#REF!`、`#N/A`、`#VALUE!`、`#DIV/0!`、`#NAME?`、`#NUM!`；存在非预期错误时停止生成月度中间表。
11. 输出站点、净需求行数、计划目标类目数、库存行数、入库单行数、在途批次 SKU 数、公式数量、公式错误数和 Excel 计算耗时。

---

## 六、更新结果表 `运营大盘数据监控表.xlsx`

### 6.1 更新顺序

1. 确认月度中间表已经通过公式缓存校验。
2. 备份现有结果表。
3. 保留并更新 `产品动态分级表`。
4. 更新 `A1类目销售数据表`。
5. 更新 `A2类目SKU销售监控表`。
6. 更新 `B2店铺类目销售表`。
7. 更新 `B1运营类目销售表`，因为 B1 的目标字段引用 B2。
8. 调用 Excel 全量计算、保存并关闭。
9. 重新扫描公式、缓存错误、月份和粒度。

### 6.2 通用历史块规则

1. 维度字段固定在左侧，当前月份指标块位于历史指标块左侧。
2. 若当前块月份不等于本次目标月份，将当前块整体复制为最左侧历史块，再写入新当前块。
3. 同一月份不得在当前块和历史块中重复。
4. 历史块从左到右按月份从晚到早排列。
5. 月份列必须整列写同一月份，禁止序列填充。
6. 新增粒度行时复制相邻有效行样式、公式结构、条件格式、行高和数字格式。
7. 手工维护字段迁移历史块时必须复制值，不得被空值覆盖。
8. 每个历史块的增长系数只能引用其右侧更早月份；最右侧最早月份没有可比基期时，增长系数必须置空，禁止生成 `#REF!`。
9. 完成率、增长系数和占比公式统一增加 `IFERROR` 或分母非零判断。
10. A1、A2、B1、B2 的有效粒度和顺序以运营大盘粒度表对应 Sheet 为准；结果表已有但粒度表已删除的旧粒度不得删除，统一追加在有效粒度末尾。
11. 追加的旧粒度仍生成本次月份和季度；本次数据指标置空，历史块完整保留。人工维护字段按业务键迁移并保留。
12. 产品动态分级表不随粒度表增删或重排，始终保留结果表现有粒度和顺序。

### 6.3 `产品动态分级表`

1. 该 Sheet 是结果表内部维表，向 A1、A2、B1、B2 提供 `产品分级`。
2. 主要粒度为 `站点 + 产品名称`，最终返回字段为 `最终定级`；粒度、行数和顺序以结果表现有内容为准，不按粒度表调整。
3. `销售额占比` 从结果表 `A1类目销售数据表 / 平均销售额占比` 按站点和产品汇总。
4. 销售额/销量、毛利率、净利率、周转率、平台适配等评分先乘权重，再汇总为 `综合分`；综合分映射为 S/A/B/C 等级。
5. 所有评分公式必须使用 `IFERROR` 或先判断输入为空；不得出现当前文件中已有的 `#DIV/0!`。
6. `产品主类/材质`保留结果表现有文本值，不再生成或恢复任何外部工作簿公式；结果表不得包含 `[1]产品动态分级表（初稿）` 等外部引用。
7. 当前样本有 45 个 `产品主类/材质` 公式引用外部工作簿；这是模板遗留链接，不属于月度中间表输入链路。

### 6.4 `A1类目销售数据表`

粒度：`站点 + 主SKU分类`。维度字段为：`供应商`、`站点`、`产品分级`、`产品主类/材质`、`主SKU分类`、`平均销售额占比`。

当前块字段：`月份`、`季度`、`目标`、`销量`、`目标完成率`、`销售额`、`目标增长系数`、`销售额增长系数`、`销售额占比`、`计划目标`、`实际库存`、`备货准确率`、`周转系数`。

| 字段 | 逻辑 |
|---|---|
| `产品分级` | 按 `站点 + 主SKU分类` 从 `产品动态分级表 / 最终定级` 匹配，未匹配返回 `未定级` |
| `平均销售额占比` | 对当前块及所有有效历史块的 `销售额占比` 求非空平均 |
| `月份` | `DATA_MONTH_CN`，整列固定 |
| `目标` | 按 `站点 + 主SKU分类`迁移并保留原手工值；粒度表新增粒度置空；粒度表删除的旧粒度追加末尾并保留人工值 |
| `销量`、`销售额`、`计划目标`、`实际库存`、`备货准确率`、`周转系数` | 读取月度中间表 A1 的 Excel 缓存值 |
| `目标完成率` | `IFERROR(销量/目标,"")`；当前模板直接除法会在目标为空或 0 时产生 `#DIV/0!`，生成时必须替换 |
| `目标增长系数` | `IFERROR(当前目标/最近历史目标,"")` |
| `销售额增长系数` | `IFERROR(当前销售额/最近历史销售额,"")` |
| `销售额占比` | `IFERROR(当前销售额/SUMIFS(当前销售额列,站点列,当前站点),0)`；每个历史块必须引用自身销售额列 |

历史销售额占比必须引用历史销售额列自身。例如历史销售额在 `Y` 列时：

```excel
=IFERROR(Y2/SUMIFS(Y:Y,B:B,B2),0)
```

禁止沿用错误公式 `=Y2/SUMIFS(L:L,B:B,B2)`。

### 6.5 `A2类目SKU销售监控表`

粒度：`站点 + 产品编码`。维度字段：`供应商`、`站点`、`产品分级`、`产品主类/材质`、`主SKU分类`、`产品编码`、`产品名称`。

当前块和每个历史块均包含：`月份`、`季度`、`计划目标`、`实际销量`、`目标完成率`、`实际库存`、`备货准确率`、`周转系数`。

1. 当前块读取月度中间表 A2 的 Excel 缓存值。
2. `产品分级` 按 `站点 + 主SKU分类` 从 `产品动态分级表 / 最终定级` 匹配。
3. 当前块月份固定为 `DATA_MONTH_CN`，不得出现逐行递增的 Excel 日期序列。
4. `目标完成率`、`备货准确率` 必须为空值保护，不得复制中间表错误缓存。

### 6.6 `B2店铺类目销售表`

粒度：`站点 + 店铺名 + 主SKU分类`。维度字段：`站点`、`店铺名`、`运营`、`产品分级`、`产品主类/材质`、`主SKU分类`。

当前块和每个历史块固定包含 12 个字段：`月份`、`季度`、`实际库存`、`客单价（RMB）`、`实际销量`、`目标销量（手填）`、`销量完成率`、`实际销售额`、`目标销售额`、`销售额完成率`、`销量增长系数`、`销售额增长系数`。

| 字段 | 逻辑 |
|---|---|
| `月份` | `DATA_MONTH_CN`，整列固定 |
| `实际库存`、`客单价（RMB）`、`实际销量`、`实际销售额` | 读取月度中间表 B2 的 Excel 缓存值 |
| `目标销量（手填）` | 按 `站点 + 店铺名 + 主SKU分类`迁移并保留当前月份已有人工值；新增粒度置空；旧粒度追加末尾并保留人工值；历史块随月份一起右移 |
| `目标销售额` | `IFERROR(目标销量（手填）*客单价（RMB）,0)` |
| `销量完成率` | `IFERROR(实际销量/目标销量（手填）,"")` |
| `销售额完成率` | `IFERROR(实际销售额/目标销售额,"")` |
| `销量增长系数` | `IFERROR(当前实际销量/右侧最近历史块实际销量,"")` |
| `销售额增长系数` | `IFERROR(当前实际销售额/右侧最近历史块实际销售额,"")` |

当前样本最右侧历史块的 `销量增长系数` 共有 251 行使用 `=历史实际销量/#REF!`。该列没有更早月份可比时必须整列置空；不得尝试保留失效引用。

### 6.7 `B1运营类目销售表`

粒度：`站点 + 运营 + 主SKU分类`。维度字段：`站点`、`产品分级`、`产品主类/材质`、`主SKU分类`、`运营`。

当前块和历史块包含：`月份`、`季度`、`实际销量`、`目标销量`、`销量完成率`、`实际销售额`、`目标销售额`、`销售额完成率`、`目标增长系数`、`销售额增长系数`、`销售额占比`。

| 字段 | 逻辑 |
|---|---|
| `月份` | `DATA_MONTH_CN`，整列固定 |
| `实际销量` | 读取月度中间表 B1 `实际销量` |
| `目标销量` | 按 `站点 + 运营 + 主SKU分类` 汇总 B2 当前块 `目标销量（手填）` |
| `销量完成率` | `IFERROR(实际销量/目标销量,"")` |
| `目标销售额` | 按相同粒度汇总 B2 当前块 `目标销售额` |
| `实际销售额` | 读取月度中间表 B1 `实际销售额` |
| `销售额完成率` | `IFERROR(实际销售额/目标销售额,"")` |
| `目标增长系数` | `IFERROR(当前目标销量/最近历史目标销量,"")` |
| `销售额增长系数` | `IFERROR(当前目标销售额/最近历史目标销售额,"")` |
| `销售额占比` | 读取月度中间表 B1，或按同站点同运营重算 |

### 6.8 结果表保存与复算

1. 保存结果表后调用 Excel 全量计算、保存并关闭。
2. 重新读取公式和缓存值，扫描全部 5 个 Sheet。
3. 若存在公式错误、外部链接、逐行递增月份、重复粒度或月份块顺序错误，则本次执行失败。
4. 结果表文件修改时间应晚于月度中间表公式激活保存时间。
5. 所有当前块和历史块中字段名包含`完成率`、`增长率`、`增长系数`、`占比`、`备货准确率`的字段，数字格式统一设置为`0.00%`。
6. `产品动态分级表`中字段名包含`占比`、`毛利率`、`净利率`、`周转率`、`权重`的字段，数字格式统一设置为`0.00%`；评分、加权分、综合分和等级字段不使用百分比格式。

---

## 七、输出校验

### 7.1 月度中间表

1. 15 个 Sheet 全部存在，名称与顺序正确。
2. A1 为 14 列，A2 为 15 列，B1 为 10 列，B2 为 12 列；A1/B1/B2 口径表结构正确，A2 口径表不得在未确认来源时伪造数据。
3. A1 粒度 `站点 + 主SKU分类` 无重复。
4. A2 粒度 `站点 + 产品编码` 无重复。
5. B1 粒度 `站点 + 运营 + 主SKU分类` 无重复。
6. B2 粒度 `站点 + 店铺名 + 主SKU分类` 无重复。
7. A1/A2/B1/B2 月份唯一值均为 `DATA_MONTH_CN`。
8. 不存在脚本新生成的公式错误缓存；允许 `净需求-目标表` 原样保留分站点净需求源字段已有的 `#N/A`。
9. 映射未命中记录已单独输出，不得以 `#N/A` 进入分析主表。
10. `净需求-目标表` 固定为 39 列，字段顺序为 A:AM；V:Y 为本表新增公式列，AL:AM 为从分站点净需求表辅助列读取的缓存值，其余字段不得包含从源净需求表复制来的公式或外部链接。
11. `净需求-目标表` 有效数据行数必须等于 `菲律宾、马来、泰国、印尼1` 四份 `净需求表` 动态识别表头后的有效数据行数之和，且 `站点(CN) + 产品编码` 不得重复；不得包含 `印尼2` 数据。
12. V:Y 四列的公式数量必须分别等于 `净需求-目标表` 有效数据行数；X、Y 的 `SUMIFS` 必须同时包含站点和主SKU分类两个条件。
13. `类目目标`、`SKU目标` 分组下的主窗口月份字段必须唯一且覆盖 `M+1` 至 `M+6`；末尾辅助列必须唯一覆盖执行月份 `M` 的类目目标和 SKU 目标。字段缺失、重复或月份错序时停止执行，不得按列位置强行改名；但 `计划目标表` 缺少某个月份目标列时不阻断，需打印缺失月份并将对应目标值按 `0` 写入。

### 7.2 在途批次表

1. 12 个 Sheet 全部存在，名称和最终顺序正确。
2. `千易入库单_新建` 为 8 列，`A未发数据表` 为 7 列，`入库单表` 为 10 列，五张批次表各 17 列。
3. `千易入库单_新建` 行数等于千易新建源数据有效行数；`入库单表` 行数等于 `千易入库单_新建` 与 `A未发数据表` 有效行数之和，A 未发数据只能追加一次。
4. 菲律宾 A 未发行的 `商品编码` 不以 `HSTEST-` 开头，且去前缀后的 SKU 非空；其他站点 SKU 未被改写。
5. 五张批次表分别只包含泰国、印尼1、印尼2、马来、菲律宾对应的唯一 SKU，不缺失、不串站点；印尼1和印尼2必须按 `站点_批次`独立校验。
6. 每个 SKU 的批次按预计到货时间、交货时间从早到晚排列，数量和入库单号聚合正确。
7. `飞书-入库单跟进表 / 到货天数` 不含失效引用。
8. 不存在任何公式错误缓存；超过四批的数据已输出日志。

### 7.3 分站点净需求计算表

1. 每个站点工作簿 10 个 Sheet 全部存在，名称、顺序和字段结构正确。
2. 六张外部数据 Sheet 的来源分别为：映射表 `SKU信息表`、映射表 `仓库信息表`、千易 `入库单明细_收货中_BATCH_F`、千易 `库存查询_BATCH_F`、在途批次表 `入库单表`、对应 `SKU批次表_{{站点_批次}}`。
3. `千易库存数据`、`千易入库单_收货中`、`入库单信息`、`在途批次数据` 不包含其他站点批次数据。
4. `净需求表` 产品编码唯一，公式覆盖所有有效产品行；主目标月份窗口为 `M+1` 至 `M+6`，库存窗口为 `M` 至 `M+3`，且 `P:U` 按目标月份引用 `计划目标表` 的真实列；末尾辅助列按执行月份 `M` 引用 `计划目标表` 的真实列。
5. 人工目标、产品分级、新品/老品、历史销量窗口和包装取整规则未被空值覆盖。
6. `货值情况` 与 `净需求表` 按主SKU分类汇总结果一致。
7. `计划目标表` 缺少 `M`、`M+1` 至 `M+6` 任一目标月份时，必须在控制台打印缺失月份，对应目标值按 `0` 写入并继续执行；如目标月份重复，则必须在写入输出前停止执行。
7. Excel 已完成全量计算并保存，不存在非预期公式错误或失效外部链接。若某错误来自模板中同位置、同公式的既有业务公式，且不是本次脚本新增或改写公式造成的，可按模板原有错误记录并放行；脚本需输出错误缓存统计。

### 7.4 运营大盘

1. 5 个 Sheet 全部存在。
2. A1、A2、B1、B2 行数至少覆盖对应中间表全部有效粒度。
3. 当前块与历史块月份列在每个块内只有一个月份值。
4. 当前月份与中间表口径一致，历史月份从晚到早。
5. A1 历史销售额占比分母引用对应历史销售额列。
6. 不存在 `[工作簿名.xlsx]Sheet` 形式的失效外部引用。
7. 不存在 `#REF!`、`#N/A`、`#VALUE!`、`#DIV/0!`、`#NAME?`。
8. 手工字段未被空值覆盖。
9. B2 最右侧最早月份的增长系数为空，不存在 `#REF!`。
10. 产品动态分级表不存在外部工作簿公式引用。

### 7.5 阶段日志

至少打印：

1. 参数和月份变量。
2. 每个源文件校验结果。
3. 每个中间表 Sheet 的行数、列数、公式列和未匹配数。
4. 月度中间表保存、Excel 计算、缓存校验结果。
5. 在途批次表每个 Sheet 的来源行数、输出行数、公式列、未命中数、超过四批数和错误数。
6. 在途批次表保存、Excel 计算、缓存校验结果。
7. 每个分站点净需求表 10 个 Sheet 的输入行数、输出行数、公式数量、保留人工字段数和未匹配数。
8. 分站点净需求表保存、Excel 计算、缓存校验和公式错误扫描结果。
9. 每个结果表 Sheet 的新增粒度数、更新粒度数、历史块移动情况和公式错误数。
10. 结果表保存、Excel 计算、最终校验结果。

---

## 八、待确认清单

1. **已确认：净需求表产品粒度**：保留各站点净需求模板现有产品编码、顺序和行数，不根据 `SKU信息表` 自动新增或删除产品。
2. **已确认：销量占比历史窗口**：保留模板历史值，不自动删除或重排。
3. **已确认：计划目标管理输入**：保留模板中的产品分级、月度目标和实际完成值，不清空重建。
4. **已确认：A1/A2 计划目标月份**：销售月份和计划目标月份均使用 `M`。
5. **已确认：实际库存口径**：A1、A2、B2 的 `实际库存` 保持 `库存合计=可用量+实际在途量`。
6. **B2 目标销量（手填）**：请确认该字段继续由人工维护，还是后续由净需求目标分摊到店铺。
7. **B1 指标命名**：当前 `目标完成率=上月销量/目标销量`、`销售额完成率=上月销售额/目标销售额`；请确认是否保留“上月”表头。
9. **产品动态分级外部来源**：当前 `产品动态分级表` 有 45 个 `产品主类/材质` 公式引用外部 `[1]产品动态分级表（初稿）`。需确认内部替代来源，或允许将现有缓存值固化为静态值。
10. **产品动态分级评分阈值**：当前销售额占比评分在不同站点区段使用不同阈值，毛利率/净利率/周转/平台适配中也有手工权重。请确认这些分段阈值和权重是否作为长期固定规则。

在上述口径确认前，程序可以完成结构、公式错误保护、月份不递增、历史块引用修复和日志校验，但不应擅自改变业务指标定义。
