# 结果表生成提示词与中文逻辑脚本

本文档用于指导自动化程序生成 `结果表/每日销售及库存跟进表.xlsx`。文档只描述提示词和中文执行逻辑，不直接提供代码。

---

## 一、可直接使用的提示词

请你作为 Excel 自动化工程师，基于给定执行日期 `D`，生成 `结果表/每日销售及库存跟进表.xlsx`，并同时保留生成过程中的 `数据中间表/每日数据中间表/每日数据中间表_D.xlsx`。

### 输入参数

- 根目录：`ROOT`，该目录下必须包含 `源数据`、`数据中间表`、`结果表` 文件夹。
- 执行日期：`D`，格式为 `YYYY-MM-DD`。
- 销售数据日期：`D-1`。
- 累计销售数据日期范围：用于 C1/C2 运营目标跟踪累计销量，按执行日期 `D` 分三种情况取源文件，详见“输入文件路径规则”。
- 库存数据日期：`D`。
- 入库单数据日期：`D`。
- 飞书入库单跟踪表日期：`D`。
- 销售日期列名：将 `D-1` 转为 `YYYY.MM.DD销量`，例如给定日期为 `2026-06-06` 时，销售日期列名为 `2026.06.05销量`。
- 类目涨幅列名：与每个销量日期一一对应，格式为 `YYYY.MM.DD涨幅`。

### 输入文件路径规则

- 商品销售统计：`ROOT/源数据/千易销售数据/商品销售统计_D-1.xlsx`
- 商品销售统计_累计：按执行日期 `D` 分三种情况取文件：
  - 若 `D` 为每月 2 号，例如 `2026-07-02`，累计销量数据日期为当月 1 号，直接读取日表 `ROOT/源数据/千易销售数据/商品销售统计_YYYY-MM-01.xlsx`，不读取累计命名文件。
  - 若 `D` 为每月 1 号，例如 `2026-07-01`，累计销量数据日期为上月最后一天，读取上月月表 `ROOT/源数据/千易销售数据/商品销售统计_YYYY-MM.xlsx`。
  - 除以上两种情况外，累计销量数据日期范围为当月 1 号至 `S=D-1`，读取 `ROOT/源数据/千易销售数据/商品销售统计_累计_YYYY-MM-01_S.xlsx`。
- 库存查询数据：`ROOT/源数据/千易库存数据/库存查询_D.xlsx`
- 新建入库单：`ROOT/源数据/千易入库单数据/入库单明细_新建_D.xlsx`
- 收货中入库单：`ROOT/源数据/千易入库单数据/入库单明细_收货中_D.xlsx`
- 飞书入库单跟踪表：`ROOT/源数据/飞书人工维护数据/入库单跟踪表/入库单跟踪表_D.xlsx`
- 映射表：`ROOT/源数据/映射表/SKU、店铺、仓库映射表.xlsx`
- 每日数据粒度表：取 `ROOT/源数据/**/@数据粒度表_每日销售及库存跟进表*.xlsx` 中修改时间最新的文件。
- 历史结果表：`ROOT/结果表/每日销售及库存跟进表.xlsx`，如果存在则读取并保留历史日期销量列；如果不存在则新建。

### 输出文件

- 必须生成并保留：`ROOT/数据中间表/每日数据中间表/每日数据中间表_D.xlsx`
- 必须生成或更新：`ROOT/结果表/每日销售及库存跟进表.xlsx`

### 强制要求

1. 开始执行前必须获得根目录 `ROOT` 和执行日期 `D`。
2. `ROOT` 下必须存在或可创建 `源数据`、`数据中间表`、`结果表` 文件夹；其中 `源数据` 必须已存在。
3. 商品销售统计取 `D-1` 文件，库存查询、入库单明细、飞书入库单跟踪表均取 `D` 文件。
4. `每日数据中间表_D.xlsx` 必须保留，且中间表中的公式必须保留为 Excel 公式，不允许只写入公式计算后的值。
5. 中间表必须按引用顺序逐个生成 sheet，再逐个生成指标，不能先生成依赖下游 sheet 的公式。
6. 脚本执行过程中的文件名、sheet 页名、字段名必须严格沿用源数据、中间表、结果表中的实际命名，不允许自动改名、同义改名、繁简替换、大小写变更、空格增删或符号替换。
7. 三个日销中间表中的销量字段不能写成字面量 `{{前一天}}销量`；必须将 `前一天` 替换为给定日期 `D` 的前一天 `S`，字段格式为 `YYYY.MM.DD销量`，例如 `2026.06.05销量`。
8. 结果表中涉及当前日期销量的逻辑，也必须使用销售日期 `S` 对应的 `YYYY.MM.DD销量` 字段名。
9. `类目日销` 中每一个日期销量列都必须配套一个同日期涨幅列，包含历史日期销量列；即所有 `YYYY.MM.DD销量` 都要有对应的 `YYYY.MM.DD涨幅`。
10. 五张数据口径必须从最新的 `@数据粒度表_每日销售及库存跟进表*.xlsx` 复制指定字段生成：`SKU日销数据口径←SKU日销（管理）`、`类目日销数据口径←类目日销（管理）`、`运营日销数据口径←运营日销`、`C1运营类目目标跟踪数据口径←C1运营类目目标跟踪`、`C2运营店铺目标跟踪数据口径←C2运营店铺目标跟踪`。
11. 结果表生成前必须与本次已经生成并完成 Excel 计算的五张数据口径表做全量有序粒度比对。结果表的行集合、顺序及维度值以每日数据中间表为准；粒度集合或顺序不同则按数据口径表重建行，并按粒度键迁移已有数据和全部历史日期列，禁止按原行号搬移数据。
12. 如果字段属于目标表逻辑位置，但字段内容为空白或没有公式，仍需保留该字段；该字段逻辑记为 `置空`。
13. 结果表中的历史日期销量列和历史日期涨幅列必须保留，不得覆盖或删除。
14. 输出前需要检查关键 sheet 是否存在、关键字段是否存在、关键公式是否保留。
15. `每日数据中间表_D.xlsx` 中，`千易商品销售统计`、`千易入库单_收货中`、`飞书-入库单跟进表`、`千易入库单_新建`、`千易库存数据` 这几个 sheet 页里由脚本新增的字段列，如果该列写入的是 Excel 公式，则该字段列的表头和公式数据区域必须使用浅蓝色背景。
16. 读取源文件时不能只校验文件存在；还必须校验首个 sheet 至少包含有效数据行。若源文件只有单个表头或 1 行 1 列，应停止执行并提示源文件疑似为空。
17. 对于多层表头源文件，必须按真实业务字段所在行识别字段，不能固定使用第 1 行作为表头。
18. 结果表中的历史销量字段日期从左往右必须按从晚到早排序；每次运行新增最新的 `YYYY.MM.DD销量` 列时，必须插入到历史销量区域的最左侧，而不是追加到最右侧。
19. `类目日销` 的每个 `YYYY.MM.DD销量` 后必须紧跟同日期 `YYYY.MM.DD涨幅`；若重新生成涨幅列，应先清理重复或孤立的涨幅列，再按销量列顺序重建。
20. 结果表不允许写入会失效的外部工作簿引用公式，例如 `='[3]SKU日销-中间'!F2` 或包含 `#REF` 的公式。`SKU日销`的供应商、四个单价及刷新指标均写入中间表 Excel 公式缓存静态值；允许保留的内部公式包括 12 个30/60/90天派生金额公式、`类目日销`汇总公式和涨幅公式，且只能引用当前结果工作簿内部 Sheet。
21. 结果表粒度字段必须是静态有效值。`站点`、`SKU`、`主SKU分类`、`运营` 等关键粒度字段如果为空、以 `=` 开头、包含 `#REF`、`#N/A`、`#VALUE!`、`#NAME?`，不得新增到结果表。
22. 中间表保存后，必须由 Python 脚本调用本机 Microsoft Excel 打开 `每日数据中间表_D.xlsx`，执行全量公式计算，保存并关闭该工作簿。这是结果表生成的硬性前置步骤；如果 Excel 启动失败、计算失败、保存失败、超时，或公式缓存不可读取，必须停止执行，不允许继续生成结果表。
23. 结果表新增或重建日期列时必须继承相邻历史日期列表头样式和列宽；`类目日销` 新增 `YYYY.MM.DD涨幅` 表头时，应复制左侧同日期销量列表头样式，避免出现默认空白表头样式。
24. 结果表`SKU日销`、`运营日销`当前日期`YYYY.MM.DD销量`读取对应中间表的 Excel 公式缓存；结果表`类目日销`当前及历史销量改为在结果表内部按`站点 + 主SKU分类`汇总`SKU日销`同名日期列并保留公式，不得由 Python 预计算静态值。结果表`C1运营类目目标跟踪`和`C2运营店铺目标跟踪`的当前`YYYY.MM.DD累计销量`读取对应中间表的 Excel 公式缓存并写入静态值。
25. Python 脚本执行过程中必须阶段性 `print` 输出日志：源文件校验完成、中间表每个 sheet 生成完成、结果表每个 sheet 更新完成、文件保存完成、输出校验完成都需要打印。
26. 三个日销中间表的 `产品主类/材质` 必须保留为 Excel 查找公式，未匹配时保留公式返回的 `#N/A`；但结果表中的同名维度统一由对应数据口径表静态写入，中间表指标公式及其缓存结果不得覆盖结果表维度。
27. `SKU日销-中间` 的 `当日库存`、`月销量`、`30天内到货在途数量` 必须按 `站点 + SKU` 使用 `SUMIFS` 汇总全部匹配库存记录，不得使用 `INDEX + MATCH` 只取第一条记录。

### 可复用信息

- 可复用 `数据中间表生成逻辑.md` 作为字段口径和公式口径说明。
- 可复用已有 `ROOT/数据中间表/每日数据中间表/每日数据中间表_历史日期.xlsx` 的 sheet 结构和公式布局作为模板，但数据日期、引用文件和行数必须按本次 `D` 重新生成。
- 可复用已有 `ROOT/结果表/每日销售及库存跟进表.xlsx` 的 sheet 样式、历史日期销量列和展示顺序。
- 可复用 `ROOT/源数据/映射表/SKU、店铺、仓库映射表.xlsx` 中的 `SKU信息表`、`店铺信息表`、`仓库信息表`。
- 当前 `飞书操作脚本/add_exchange_rate_record.py` 是汇率维护脚本，与本次销售库存结果表生成无直接关系，不作为本流程核心复用脚本。

---

## 二、中文逻辑脚本

### 0. 执行入口

1. 接收根目录 `ROOT` 和执行日期 `D`。
2. 校验 `ROOT` 是否存在。
3. 校验 `ROOT/源数据` 是否存在；如果不存在，停止执行。
4. 校验 `ROOT/数据中间表`、`ROOT/结果表` 是否存在；如果不存在，则创建对应文件夹。
5. 校验 `D` 是否为合法日期，格式必须为 `YYYY-MM-DD`。
6. 计算销售日期 `S = D - 1 天`。
7. 将 `S` 转为销量日期列格式 `YYYY.MM.DD销量`，例如 `S=2026-06-05` 转为 `2026.06.05销量`。
8. 将 `S` 转为类目涨幅列格式 `YYYY.MM.DD涨幅`，例如 `S=2026-06-05` 转为 `2026.06.05涨幅`。
9. 本流程中所有“日销销量日期列”均使用 `S`，不是使用执行日期 `D`。
10. 定义输出中间表路径：`ROOT/数据中间表/每日数据中间表/每日数据中间表_D.xlsx`。
11. 定义输出结果表路径：`ROOT/结果表/每日销售及库存跟进表.xlsx`。
12. 定义结果表日期列插入规则：最新 `YYYY.MM.DD销量` 必须插入到固定基础字段之后、历史销量字段之前；`类目日销` 同时插入 `YYYY.MM.DD销量` 与 `YYYY.MM.DD涨幅` 两列。

### 1. 输入文件校验

逐一检查以下文件是否存在：

| 文件类型 | 日期口径 | 路径 |
|---|---|---|
| 商品销售统计 | `S = D-1` | `ROOT/源数据/千易销售数据/商品销售统计_S.xlsx` |
| 商品销售统计_累计 | 分三种情况：`D` 为每月 2 号取当月 1 号日表；`D` 为每月 1 号取上月月表；其他日期取当月 1 号至 `S=D-1` 的累计表 | 2号：`ROOT/源数据/千易销售数据/商品销售统计_YYYY-MM-01.xlsx`；1号：`ROOT/源数据/千易销售数据/商品销售统计_YYYY-MM.xlsx`；其他：`ROOT/源数据/千易销售数据/商品销售统计_累计_YYYY-MM-01_S.xlsx` |
| 库存查询 | `D` | `ROOT/源数据/千易库存数据/库存查询_D.xlsx` |
| 新建入库单 | `D` | `ROOT/源数据/千易入库单数据/入库单明细_新建_D.xlsx` |
| 收货中入库单 | `D` | `ROOT/源数据/千易入库单数据/入库单明细_收货中_D.xlsx` |
| 飞书入库单跟踪表 | `D` | `ROOT/源数据/飞书人工维护数据/入库单跟踪表/入库单跟踪表_D.xlsx` |
| 映射表 | 固定 | `ROOT/源数据/映射表/SKU、店铺、仓库映射表.xlsx` |
| 每日结果表数据粒度表 | 取文件修改时间最新版本 | `ROOT/源数据/**/@数据粒度表_每日销售及库存跟进表*.xlsx` |

如果任一必需源文件不存在，停止执行并明确提示缺失文件路径。
如果任一必需源文件疑似为空，例如首个 sheet 没有有效数据行，也必须停止执行并明确提示缺失有效数据的文件路径。注意：部分千易导出的 xlsx 会把 worksheet dimension 元数据错误写成 `A1`，脚本不能只依赖只读模式的 `max_row/max_column` 或 dimension 元数据判断是否为空，必须实际读取前若干行数据单元格确认。

### 1.1 命名规则校验

1. 所有读取到的源数据 sheet 名、字段名，必须按源 Excel 中的真实文本使用。
2. 所有生成的中间表 sheet 名必须严格为：
   - `SKU日销-中间`
   - `类目日销-中间`
   - `运营日销-中间`
   - `C1运营类目目标跟踪-中间表`
   - `C2运营店铺目标跟踪-中间表`
   - `SKU日销数据口径`
   - `类目日销数据口径`
   - `运营日销数据口径`
   - `C1运营类目目标跟踪数据口径`
   - `C2运营店铺目标跟踪数据口径`
   - `千易商品销售统计`
   - `千易商品销售统计_累计`
   - `千易库存数据`
   - `千易入库单_新建`
   - `千易入库单_收货中`
   - `飞书-入库单跟进表`
   - `SKU信息表`
   - `店铺信息表`
   - `仓库信息表`
3. 所有生成的结果表 sheet 名必须严格为：
   - `SKU日销`
   - `类目日销`
   - `运营日销`
   - `C1运营类目目标跟踪`
   - `C2运营店铺目标跟踪`
4. 所有字段名必须严格使用本文档指定字段名；日期销量字段必须使用销售日期 `S` 的 `YYYY.MM.DD销量` 格式，日期涨幅字段必须使用 `YYYY.MM.DD涨幅` 格式。
5. 不允许将 `销售sku数量VALUE` 改成 `销售SKU数量VALUE`、`销售sku数量值` 等相似名称。
6. 不允许将 `站点(CN)`、`站点（中文）`、`站点（英文）` 混用。

### 2. 创建每日数据中间表工作簿

创建 `ROOT/数据中间表/每日数据中间表/每日数据中间表_D.xlsx`。

生成 sheet 的顺序必须遵循依赖关系：

1. `SKU信息表`
2. `店铺信息表`
3. `仓库信息表`
4. `千易商品销售统计`
5. `千易商品销售统计_累计`
6. `千易入库单_收货中`
7. `飞书-入库单跟进表`
8. `千易入库单_新建`
9. `千易库存数据`
10. `SKU日销数据口径`
11. `类目日销数据口径`
12. `运营日销数据口径`
13. `C1运营类目目标跟踪数据口径`
14. `C2运营店铺目标跟踪数据口径`
15. `SKU日销-中间`
16. `类目日销-中间`
17. `运营日销-中间`
18. `C1运营类目目标跟踪-中间表`
19. `C2运营店铺目标跟踪-中间表`

说明：基础数据和映射数据必须先生成；五张数据口径表是结果表维度和排序的权威来源；五张中间指标表供结果表读取 Excel 公式缓存。

---

## 三、每日数据中间表生成步骤

### 3.1 生成 `SKU信息表`

1. 从 `ROOT/源数据/映射表/SKU、店铺、仓库映射表.xlsx / SKU信息表` 原样复制到中间表。
2. 保留全部字段。
3. 不新增公式。

### 3.2 生成 `店铺信息表`

1. 从 `ROOT/源数据/映射表/SKU、店铺、仓库映射表.xlsx / 店铺信息表` 原样复制到中间表。
2. 保留字段：`国家`、`店铺`、`运营`、`站点`。
3. 不新增公式。

### 3.3 生成 `仓库信息表`

1. 从 `ROOT/源数据/映射表/SKU、店铺、仓库映射表.xlsx / 仓库信息表` 原样复制到中间表。
2. 保留字段：`仓库代码`、`对应站点`、`站点缩写`。
3. 不新增公式。

### 3.4 生成 `千易商品销售统计`

1. 从 `ROOT/源数据/千易销售数据/商品销售统计_S.xlsx` 复制销售统计数据。
2. 保留源表销售字段，其中源表已有 `站点` 字段，必须保留源表原值，不允许用公式覆盖。
3. 新增或确保以下字段存在：

| 字段 | 逻辑 |
|---|---|
| `主SKU分类` | 按 `系统sku` 到 `当前excel / SKU信息表` 匹配 `主SKU分类` |
| `运营` | 按 `店铺` 到 `当前excel / 店铺信息表` 匹配 `运营` |
| `站点(CN)` | 按 `店铺` 到 `当前excel / 店铺信息表` 匹配中文站点 |
| `销售sku数量VALUE` | 将 `销售sku数量` 转为数值，逻辑为 `销售sku数量 + 0` |

4. 上述字段必须写入 Excel 公式，不允许只写结果值。

### 3.4.1 生成 `千易商品销售统计_累计`

1. 该 Sheet 用于承载 C1/C2 运营目标跟踪的累计销量源数据。无论读取哪一种源文件，最终生成的累计销量字段名都使用销售日期 `S=D-1`，格式为 `YYYY.MM.DD累计销量`。
2. 源文件按执行日期 `D` 分三种情况读取：
   - 当 `D` 为每月 2 号时，累计销量数据日期为当月 1 号，直接读取日表 `ROOT/源数据/千易销售数据/商品销售统计_YYYY-MM-01.xlsx`。示例：`D=2026-07-02` 时读取 `商品销售统计_2026-07-01.xlsx`。
   - 当 `D` 为每月 1 号时，累计销量数据日期为上月最后一天，读取上月月表 `ROOT/源数据/千易销售数据/商品销售统计_YYYY-MM.xlsx`。示例：`D=2026-07-01` 时读取 `商品销售统计_2026-06.xlsx`。
   - 除以上两种情况外，累计销量数据日期范围为当月 1 号至销售日期 `S`，读取累计表 `ROOT/源数据/千易销售数据/商品销售统计_累计_YYYY-MM-01_S.xlsx`。示例：`D=2026-07-03`、`S=2026-07-02` 时读取 `商品销售统计_累计_2026-07-01_2026-07-02.xlsx`。
3. 读取到的源表字段结构与 `千易商品销售统计` 相同。
4. 保留源表销售字段，其中源表已有 `站点` 字段，必须保留源表原值，不允许用公式覆盖。
5. 新增或确保以下字段存在，字段逻辑与 `千易商品销售统计` 完全一致：

| 字段 | 逻辑 |
|---|---|
| `主SKU分类` | 按 `系统sku` 到 `当前excel / SKU信息表` 匹配 `主SKU分类` |
| `运营` | 按 `店铺` 到 `当前excel / 店铺信息表` 匹配 `运营` |
| `站点(CN)` | 按 `店铺` 到 `当前excel / 店铺信息表` 匹配中文站点 |
| `销售sku数量VALUE` | 将 `销售sku数量` 转为数值，逻辑为 `销售sku数量 + 0` |

6. 上述新增字段必须写入 Excel 公式；该 Sheet 主要供 `C2运营店铺目标跟踪-中间表` 计算 `YYYY.MM.DD累计销量`。

### 3.5 生成 `千易入库单_收货中`

1. 从 `ROOT/源数据/千易入库单数据/入库单明细_收货中_D.xlsx` 复制数据。
2. 保留收货中入库单明细。
3. 按字段名定位 `预计数量`、`仓库上架数量` 和 `批注`，字段名中如存在空格或不可见空白字符，需要先做字段名清洗后再定位。
4. 新增或确保字段 `未收到库存` 存在，不允许固定写入某个列号覆盖源字段。
5. `未收到库存` 逻辑：先判断当前行 `批注` 是否包含文本 `点完`；如果包含，则取 `MAX(预计数量 - 仓库上架数量, 0)`；如果不包含或 `批注` 为空，则返回 `0`。
6. 该字段必须保留公式，并使用浅蓝色背景。

等价公式结构：

```excel
=IF(ISNUMBER(FIND("点完",批注单元格)),MAX(预计数量单元格-仓库上架数量单元格,0),0)
```

公式必须按字段名取得实际列位置，不得固定使用 `AT`、`AC`、`AI` 或 `AU` 等列字母。

### 3.6 生成 `飞书-入库单跟进表`

1. 从 `ROOT/源数据/飞书人工维护数据/入库单跟踪表/入库单跟踪表_D.xlsx` 复制数据。
2. 保留字段：`仓库`、`站点`、`入库单号`、`发货时间`、`到仓时间`、`到仓时间整合`、`是否到货`、`到货天数`。
3. `到货天数` 以销售日期 `S` 作为固定日期基准，再与 `到仓时间整合` 做天数差，不允许按同行引用 `千易商品销售统计` 的日期字段。
4. `到货天数` 必须保留公式。

### 3.7 生成 `千易入库单_新建`

1. 从 `ROOT/源数据/千易入库单数据/入库单明细_新建_D.xlsx` 复制数据。
2. 保留核心字段：`入库单号`、`收货仓`、`商品编码`、`预计数量`。
3. 新增字段并保留公式：

| 字段 | 逻辑 |
|---|---|
| `站点（中文）` | 按 `收货仓` 到 `当前excel / 仓库信息表` 匹配中文站点 |
| `站点（英文）` | 按 `收货仓` 到 `当前excel / 仓库信息表` 匹配站点缩写 |
| `预计数量VALUE` | `预计数量 + 0` |
| `发货时间` | 按 `入库单号` 到 `当前excel / 飞书-入库单跟进表` 匹配 |
| `到仓时间` | 按 `入库单号` 到 `当前excel / 飞书-入库单跟进表` 匹配 |
| `到仓时间整合` | 按 `入库单号` 到 `当前excel / 飞书-入库单跟进表` 匹配 |
| `到货天数` | 按 `入库单号` 到 `当前excel / 飞书-入库单跟进表` 匹配；如果未匹配到，返回 `-1`，避免被纳入 60/90 天在途统计 |

### 3.8 生成 `千易库存数据`

1. 从 `ROOT/源数据/千易库存数据/库存查询_D.xlsx` 复制库存数据。
2. 该源文件为双层表头：第 1 行为分类表头，第 2 行才是真实字段名；生成时必须按第 2 行识别字段，并从第 3 行开始复制数据。
3. 保留库存核心字段：`商品编码`、`商品名称`、`仓库`、`可用量`、`在途量`、`3天销量`、`7天销量`、`15天销量`、`30天销量`、`60天销量`、`90天销量`。
4. 新增字段并保留公式：

| 字段 | 逻辑 |
|---|---|
| `主SKU分类` | 按 `商品编码` 到 `当前excel / SKU信息表` 匹配 |
| `站点（中文）` | 按 `仓库` 到 `当前excel / 仓库信息表` 匹配中文站点 |
| `站点（英文）` | 按 `仓库` 到 `当前excel / 仓库信息表` 匹配站点缩写 |
| `未收到库存` | 按 `商品编码 + 仓库` 汇总 `当前excel / 千易入库单_收货中` 的 `未收到库存`；库存表的 `仓库` 必须匹配收货中明细的 `收货仓` |
| `实际在途量` | 如果 `在途量 - 未收到库存 < 0`，则为 `0`；否则为 `在途量 - 未收到库存` |
| `库存合计` | `实际在途量 + 可用量` |
| `新建入库单数量` | 按 `商品编码 + 仓库` 汇总 `当前excel / 千易入库单_新建` 的 `预计数量VALUE`；库存表的 `仓库` 匹配新建入库单的 `收货仓` |
| `总在途（含新建入库单）` | `实际在途量 + 新建入库单数量` |

5. 上述新增公式字段列必须使用浅蓝色背景。

`未收到库存` 等价公式结构：

```excel
=SUMIFS(千易入库单_收货中!未收到库存列,
        千易入库单_收货中!商品编码列,当前行商品编码,
        千易入库单_收货中!收货仓列,当前行仓库)
```

公式必须按字段名动态定位 `未收到库存`、`商品编码`、`收货仓` 和 `仓库` 的实际列位置，不得固定依赖样表中的 `AU`、`P`、`G`、`A`、`D` 列字母。该双条件用于避免同一 SKU 在不同仓库之间互相串算未收到库存。

`新建入库单数量`与`总在途（含新建入库单）`等价公式结构：

```excel
新建入库单数量 = SUMIFS(千易入库单_新建!预计数量VALUE列,
                         千易入库单_新建!收货仓列,当前行仓库,
                         千易入库单_新建!商品编码列,当前行商品编码)

总在途（含新建入库单） = 实际在途量 + 新建入库单数量
```

两列必须保留 Excel 公式并使用浅蓝色背景。当前版本中，`SKU日销-中间 / 30天内到货在途数量`仍按`千易库存数据 / 实际在途量`汇总，不改为引用`总在途（含新建入库单）`。

### 3.9 生成 `SKU日销数据口径`

1. 数据来源：最新 `ROOT/源数据/**/@数据粒度表_每日销售及库存跟进表*.xlsx / SKU日销（管理）`。
2. 源 Sheet 第 2 行是字段表头，第 3 行开始是数据。
3. 按源表顺序复制字段：`站点(中文)`、`站点(英文)`、`产品分级`、`产品主类/材质`、`主SKU分类`、`产品编码`、`产品名称`。
4. 不从商品销售统计去重生成，不改变源表顺序；`站点(英文) + 产品编码`不得重复。
5. 该 Sheet 是`SKU日销-中间`和结果表`SKU日销`的权威粒度来源。

### 3.10 生成 `类目日销数据口径`

1. 数据来源：最新 `ROOT/源数据/**/@数据粒度表_每日销售及库存跟进表*.xlsx / 类目日销（管理）`。
2. 源 Sheet 第 2 行是字段表头，第 3 行开始是数据。
3. 按源表顺序复制字段：`站点(中文)`、`站点(英文)`、`产品分级`、`产品主类/材质`、`主SKU分类`。
4. 不从商品销售统计去重生成，不改变源表顺序；`站点(英文) + 主SKU分类`不得重复。
5. 该 Sheet 是`类目日销-中间`和结果表`类目日销`的权威粒度来源。

### 3.11 生成 `运营日销数据口径`

1. 数据来源：最新 `ROOT/源数据/**/@数据粒度表_每日销售及库存跟进表*.xlsx / 运营日销`。
2. 源 Sheet 第 2 行是字段表头，第 3 行开始是数据。
3. 按源表顺序复制字段：`站点(中文)`、`站点(英文)`、`产品分级`、`产品主类/材质`、`主SKU分类`、`运营`。
4. 不从商品销售统计去重生成，不改变源表顺序；`站点(英文) + 运营 + 主SKU分类`不得重复。
5. 该 Sheet 是`运营日销-中间`和结果表`运营日销`的权威粒度来源。

### 3.11.1 生成 `C1运营类目目标跟踪数据口径`

1. 数据来源：最新 `ROOT/源数据/**/@数据粒度表_每日销售及库存跟进表*.xlsx / C1运营类目目标跟踪`。
2. 源 Sheet 第 2 行是字段表头，第 3 行开始是数据。
3. 按源表顺序复制字段：`站点`、`产品分级`、`产品主类/材质`、`主SKU分类`、`运营`。
4. 不从商品销售统计去重生成，不改变源表顺序；`站点 + 运营 + 主SKU分类`不得重复。
5. 该 Sheet 是`C1运营类目目标跟踪-中间表`和结果表`C1运营类目目标跟踪`的权威粒度来源。

### 3.11.2 生成 `C2运营店铺目标跟踪数据口径`

1. 数据来源：最新 `ROOT/源数据/**/@数据粒度表_每日销售及库存跟进表*.xlsx / C2运营店铺目标跟踪`。
2. 源 Sheet 第 2 行是字段表头，第 3 行开始是数据。
3. 按源表顺序复制字段：`站点`、`店铺名`、`运营`、`产品分级`、`产品主类/材质`、`主SKU分类`。
4. 不从商品销售统计去重生成，不改变源表顺序；`站点 + 店铺名 + 运营 + 主SKU分类`不得重复。
5. 该 Sheet 是`C2运营店铺目标跟踪-中间表`和结果表`C2运营店铺目标跟踪`的权威粒度来源。

### 3.12 生成 `SKU日销-中间`

1. 以 `当前excel / SKU日销数据口径` 作为基础粒度。
2. 每一行粒度为 `站点 + SKU`。
3. 生成字段并保留公式：

| 字段 | 逻辑 |
|---|---|
| `供应商` | 按 `SKU` 到 `当前excel / SKU信息表` 匹配 `供应商`，保留 Excel `XLOOKUP` 公式 |
| `站点` | 来自口径表 `站点(英文)` |
| `产品分级` | 置空 |
| `产品主类/材质` | 按 `SKU` 到 `当前excel / SKU信息表` 匹配 `产品主类/材质`；使用 `XLOOKUP`，未匹配时保留 `#N/A` |
| `主SKU分类` | 来自口径表 |
| `SKU` | 来自口径表 `产品编码` |
| `货值单价` | 按 `SKU` 到 `当前excel / SKU信息表` 匹配出厂成本价 |
| `货值+海运单价` | 按 `SKU + 站点` 到 `当前excel / SKU信息表` 匹配对应国家成本+海运 |
| `账单收入单价` | 按站点由 `货值+海运单价` 推导 |
| `销售额单价` | `账单收入单价 * 1.3` |
| `当日库存` | 按 `SKU + 站点` 对 `当前excel / 千易库存数据 / 可用量` 使用 `SUMIFS` 汇总全部匹配记录 |
| `月销量` | 按 `SKU + 站点` 对 `当前excel / 千易库存数据 / 30天销量` 使用 `SUMIFS` 汇总全部匹配记录 |
| `可售月数` | `当日库存 / 月销量`，错误或为空时为 0 |
| `30天内到货在途数量` | 按 `SKU + 站点` 对 `当前excel / 千易库存数据 / 实际在途量` 使用 `SUMIFS` 汇总全部匹配记录 |
| `30天货值` | `30天内到货在途数量 * 货值单价` |
| `30天货值+海运` | `30天内到货在途数量 * 货值+海运单价` |
| `30天销售额` | `30天内到货在途数量 * 销售额单价` |
| `30天账单收入` | `30天内到货在途数量 * 账单收入单价` |
| `60天内到货在途数量` | 从 `当前excel / 千易入库单_新建` 汇总 `预计数量VALUE`，条件为 `到货天数 >= 0 且 <= 60`、`站点（英文）=站点`、`商品编码=SKU` |
| `60天货值` | `60天内到货在途数量 * 货值单价` |
| `60天货值+海运` | `60天内到货在途数量 * 货值+海运单价` |
| `60天销售额` | `60天内到货在途数量 * 销售额单价` |
| `60天账单收入` | `60天内到货在途数量 * 账单收入单价` |
| `90天在途数量` | 从 `当前excel / 千易入库单_新建` 汇总 `预计数量VALUE`，条件为 `到货天数 > 60`、`站点（英文）=站点`、`商品编码=SKU` |
| `90天货值` | `90天在途数量 * 货值单价` |
| `90天货值+海运` | `90天在途数量 * 货值+海运单价` |
| `90天销售额` | `90天在途数量 * 销售额单价` |
| `90天账单收入` | `90天在途数量 * 账单收入单价` |
| `库存状况` | 置空 |
| `YYYY.MM.DD销量` | 字段名使用销售日期 `S`，例如 `2026.06.05销量`；从 `当前excel / 千易商品销售统计` 按 `站点 + SKU` 汇总 `销售sku数量VALUE` |

### 3.13 生成 `类目日销-中间`

1. 以 `当前excel / 类目日销数据口径` 作为基础粒度。
2. 每一行粒度为 `站点 + 主SKU分类`。
3. 所有库存、销量、在途、货值、销售额、账单收入字段均从 `当前excel / SKU日销-中间` 按 `站点 + 主SKU分类` 汇总。
4. `供应商`按`主SKU分类`到`当前excel / SKU信息表`匹配供应商并保留`XLOOKUP`公式。
5. `产品分级`、`库存状况` 置空。
6. `产品主类/材质` 按 `主SKU分类` 到 `当前excel / SKU信息表` 匹配同名字段；使用 `XLOOKUP`，未匹配时保留 `#N/A`。
7. `YYYY.MM.DD销量` 字段名使用销售日期 `S`，例如 `2026.06.05销量`；该字段从 `SKU日销-中间` 按 `站点 + 主SKU分类` 汇总。

### 3.14 生成 `运营日销-中间`

1. 以 `当前excel / 运营日销数据口径` 作为基础粒度。
2. 每一行粒度为 `站点 + 运营 + 主SKU分类`。
3. `产品分级` 置空。
4. `产品主类/材质` 按 `主SKU分类` 到 `当前excel / SKU信息表` 匹配同名字段；使用 `XLOOKUP`，未匹配时保留 `#N/A`。
5. `YYYY.MM.DD销量` 字段名使用销售日期 `S`，例如 `2026.06.05销量`；该字段从 `当前excel / 千易商品销售统计` 按 `站点 + 运营 + 主SKU分类` 汇总 `销售sku数量VALUE`。

### 3.14.1 生成 `C2运营店铺目标跟踪-中间表`

1. 以 `当前excel / C2运营店铺目标跟踪数据口径` 作为基础粒度。
2. 每一行粒度为 `站点 + 店铺名 + 运营 + 主SKU分类`。
3. 维度字段按口径表静态写入：`站点`、`店铺名`、`运营`、`产品分级`、`产品主类/材质`、`主SKU分类`。
4. 新增当前累计销量字段，字段名使用销售日期 `S`，格式为 `YYYY.MM.DD累计销量`，例如执行日期 `D=2026-07-03` 时字段为 `2026.07.02累计销量`。
5. `YYYY.MM.DD累计销量` 从 `当前excel / 千易商品销售统计_累计` 按以下条件汇总 `销售sku数量VALUE`：
   - `站点(CN) = 当前行站点`
   - `店铺 = 当前行店铺名`
   - `主SKU分类 = 当前行主SKU分类`
6. 该字段必须保留 Excel `SUMIFS` 公式。

### 3.14.2 生成 `C1运营类目目标跟踪-中间表`

1. 以 `当前excel / C1运营类目目标跟踪数据口径` 作为基础粒度。
2. 每一行粒度为 `站点 + 运营 + 主SKU分类`。
3. 维度字段按口径表静态写入：`站点`、`产品分级`、`产品主类/材质`、`主SKU分类`、`运营`。
4. 新增当前累计销量字段，字段名使用销售日期 `S`，格式为 `YYYY.MM.DD累计销量`。
5. `YYYY.MM.DD累计销量` 从 `当前excel / C2运营店铺目标跟踪-中间表` 按以下条件汇总同名累计销量字段：
   - `站点 = 当前行站点`
   - `主SKU分类 = 当前行主SKU分类`
   - `运营 = 当前行运营`
6. 该字段必须保留 Excel `SUMIFS` 公式。

### 3.15 保存每日数据中间表

1. 保存 `ROOT/数据中间表/每日数据中间表/每日数据中间表_D.xlsx`。
2. 必须确保中间表中的公式仍为公式。
3. 不允许只保存公式计算结果。
4. 中间表保存后不得立即生成结果表；必须先进入 Excel 公式激活计算步骤。

---

## 四、结果表生成步骤

### 4.1 打开或创建结果表

1. 如果 `ROOT/结果表/每日销售及库存跟进表.xlsx` 已存在，则打开该文件。
2. 如果不存在，则创建新文件，并创建 `SKU日销`、`类目日销`、`运营日销`、`C1运营类目目标跟踪`、`C2运营店铺目标跟踪` 五个 sheet。
3. 如果已有结果表存在，需要保留历史日期销量列、历史日期涨幅列和历史日期累计销量列。
4. 更新前先清理已有结果表中关键粒度字段无效的污染行：粒度字段为空、为公式、或包含 `#REF` / `#N/A` / `#VALUE!` / `#NAME?` 的行不得保留为有效粒度。
5. 更新前检查日期列：历史销量日期和历史累计销量日期从左到右必须为从晚到早；本次销售日期 `S` 若不存在，应插入到对应历史日期区域最左侧。
6. 在更新五个结果表前，脚本必须先用 Python 调用本机 Microsoft Excel 打开 `ROOT/数据中间表/每日数据中间表/每日数据中间表_D.xlsx`，执行全量公式计算，保存并关闭。
7. Excel 计算和保存是硬性前置条件；未完成该步骤时，不允许打开或更新结果表。
8. Excel 保存后，脚本再以“读取公式计算后缓存值”的方式打开中间表，结果表所有同步字段均以该缓存值为准；不得用 Python 自行重算中间表公式结果。
9. 脚本必须校验 `SKU日销-中间`、`类目日销-中间`、`运营日销-中间` 的当前 `YYYY.MM.DD销量` 字段缓存可读取，并校验 `C1运营类目目标跟踪-中间表`、`C2运营店铺目标跟踪-中间表` 的当前 `YYYY.MM.DD累计销量` 字段缓存可读取：如果某个 sheet 存在数据行，但对应字段缓存可读取行数为 `0`，必须停止执行。
10. 写入当前日期销量或累计销量前，结果表该日期列应先置为 `0`，再对本次中间表存在的粒度写入缓存值，避免已有历史文件中的旧值残留。
11. 打开结果表后、同步中间表前，必须先执行第 4.2 节的数据粒度与排序对齐。
12. 结果表不再依赖内部`SKU信息表`查询单价；`SKU日销`的供应商和四个单价均读取本次中间表经 Excel 计算后的缓存静态值。

### 4.2 按每日数据中间表的数据口径对齐结果表

1. 五张数据口径 Sheet 固定使用第 1 行作为字段表头，第 2 行开始为有效粒度数据；其内容已在中间表生成阶段从最新管理粒度表复制。
2. 五张结果 Sheet 的权威行集合、维度值和排序分别来自：

| 结果表 Sheet | 中间表数据口径 Sheet | 唯一粒度键 | 维度写入映射 |
|---|---|---|---|
| `SKU日销` | `SKU日销数据口径` | `站点(英文) + 产品编码` | `站点←站点(英文)`、`产品分级`、`产品主类/材质`、`主SKU分类`、`SKU←产品编码`、`产品名称` |
| `类目日销` | `类目日销数据口径` | `站点(英文) + 主SKU分类` | `站点←站点(英文)`、`产品分级`、`产品主类/材质`、`主SKU分类` |
| `运营日销` | `运营日销数据口径` | `站点(英文) + 运营 + 主SKU分类` | `站点←站点(英文)`、`产品分级`、`产品主类/材质`、`主SKU分类`、`运营` |
| `C1运营类目目标跟踪` | `C1运营类目目标跟踪数据口径` | `站点 + 运营 + 主SKU分类` | `站点`、`产品分级`、`产品主类/材质`、`主SKU分类`、`运营` |
| `C2运营店铺目标跟踪` | `C2运营店铺目标跟踪数据口径` | `站点 + 店铺名 + 运营 + 主SKU分类` | `站点`、`店铺名`、`运营`、`产品分级`、`产品主类/材质`、`主SKU分类` |

3. 每次运行必须逐行全量比较以下内容，不能因为粒度键已存在就跳过：
   - 有序粒度键列表是否一致。
   - `SKU日销` 的 `站点`、`产品分级`、`产品主类/材质`、`主SKU分类`、`SKU`、`产品名称` 是否与 `SKU日销数据口径` 一致。
   - `类目日销` 的 `站点`、`产品分级`、`产品主类/材质`、`主SKU分类` 是否与 `类目日销数据口径` 一致。
   - `运营日销` 的 `站点`、`产品分级`、`产品主类/材质`、`主SKU分类`、`运营` 是否与 `运营日销数据口径` 一致。
   - `C1运营类目目标跟踪` 的 `站点`、`产品分级`、`产品主类/材质`、`主SKU分类`、`运营` 是否与 `C1运营类目目标跟踪数据口径` 一致。
   - `C2运营店铺目标跟踪` 的 `站点`、`店铺名`、`运营`、`产品分级`、`产品主类/材质`、`主SKU分类` 是否与 `C2运营店铺目标跟踪数据口径` 一致。
4. 如果粒度键集合和顺序一致，但任一维度字段不同，则保持当前行位置和数据指标不变，仅用对应数据口径表的值覆盖不同的维度单元格，并输出维度更新单元格数量。
5. 如果粒度键集合或顺序不一致，必须按以下规则重建结果表数据行：
   - 管理表和结果表都存在的粒度：按粒度键迁移原结果行全部数据字段、公式、样式和历史日期数据，再放到管理表指定的新位置。
   - 管理表新增的粒度：在对应位置创建新行，从管理表写入完整维度字段；数据指标和历史日期值先置空，当前日期销量随后由中间表同步，未匹配时为 `0`。
   - 管理表已经删除的粒度：从结果表有效行中移除，不得继续保留在末尾。
   - 结果表存在粒度字段为空、公式或错误值的污染行：在重建过程中移除。
6. 历史指标必须通过唯一粒度键迁移，禁止按行号、相邻位置或排序索引直接复制，否则排序变化会造成数据错位。
7. 管理表或结果表存在重复粒度时必须停止执行并报告重复键，不能静默保留其中一行。
8. 对齐结束后再次逐行比较有序粒度键和全部数据口径维度字段；任一不一致则停止执行，不得同步指标。
9. 本次每日数据中间表的五张数据口径是结果表维度字段的直接权威来源；结果表不得新增数据口径表之外的行。

### 4.3 生成或更新 `SKU日销`

1. 来源：`ROOT/数据中间表/每日数据中间表/每日数据中间表_D.xlsx / SKU日销数据口径` 和 `SKU日销-中间`。
2. 结果表粒度：`站点 + SKU`。
3. 结果表行已由`SKU日销数据口径`预先生成和排序。对每一行 `SKU日销-中间` 按 `站点 + SKU` 查找结果行；未在数据口径表出现的指标行必须跳过并输出日志，不得追加新行。
4. 维度字段：`站点`、`产品分级`、`产品主类/材质`、`主SKU分类`、`SKU`、`产品名称`，统一由`SKU日销数据口径`写入；`供应商`由`SKU日销-中间`同步。
5. 从 `SKU日销-中间` 同步的静态数据字段：
   - `供应商`
   - `货值单价`
   - `货值+海运单价`
   - `销售额单价`
   - `账单收入单价`
   - `当日库存`
   - `月销量`
   - `可售月数`
   - `30天内到货在途数量`
   - `60天内到货在途数量`
   - `90天在途数量`
   - `库存状况`
6. 同步中间表前，清空需要本次刷新的静态字段：`供应商`、四个单价、`当日库存`、`月销量`、`可售月数`、30/60/90天数量；不得清空六个维度字段、十二个30/60/90天派生公式、`库存状况`或历史销量列。
7. `货值单价`、`货值+海运单价`、`销售额单价`、`账单收入单价`均从`SKU日销-中间`读取 Excel 公式计算后的缓存值，写入结果表为静态值；结果表不再查询内部`SKU信息表`，也不保留四个单价查询公式。
8. 十二个30/60/90天金额指标必须在结果表保留内部公式：

| 批次 | 货值 | 货值+海运 | 销售额 | 账单收入 |
|---|---|---|---|---|
| 30天 | `30天内到货在途数量 × 货值单价` | `30天内到货在途数量 × 货值+海运单价` | `30天内到货在途数量 × 销售额单价` | `30天内到货在途数量 × 账单收入单价` |
| 60天 | `60天内到货在途数量 × 货值单价` | `60天内到货在途数量 × 货值+海运单价` | `60天内到货在途数量 × 销售额单价` | `60天内到货在途数量 × 账单收入单价` |
| 90天 | `90天在途数量 × 货值单价` | `90天在途数量 × 货值+海运单价` | `90天在途数量 × 销售额单价` | `90天在途数量 × 账单收入单价` |

9. `30天内到货在途数量`、`60天内到货在途数量`、`90天在途数量`只读取每日中间表经 Excel 计算后的缓存值；其后对应的货值、货值+海运、销售额、账单收入不得从中间表复制。
10. 清空完成后，再从 `SKU日销-中间` 按粒度写入静态数据字段；数据口径存在但指标表未匹配的行，本次静态刷新字段保持空白，公式字段继续保留。
11. 将销售日期 `S` 对应的 `YYYY.MM.DD销量`写入结果表同名销量列；该值来自`SKU日销-中间`同名字段经 Excel 计算后的缓存值。
12. 如果该销量列不存在，必须插入在基础字段 `库存状况` 后、所有历史销量列前。
13. `产品主类/材质`由`SKU日销数据口径`静态写入，不得复制指标表公式或缓存结果。
14. 新增销量列表头必须继承相邻历史销量列表头样式和列宽；历史日期销量列保持从左到右由晚到早。

### 4.4 生成或更新 `类目日销`

1. 来源：`每日数据中间表 / 类目日销数据口径`、`类目日销-中间`和当前结果表`SKU日销`。
2. 结果表粒度：`站点 + 主SKU分类`。
3. 结果表行由`类目日销数据口径`预先生成和排序；类目数值指标在结果表内部按`站点 + 主SKU分类`从`SKU日销`汇总。
4. 维度字段：`站点`、`产品分级`、`产品主类/材质`、`主SKU分类`由`类目日销数据口径`写入；`供应商`从`类目日销-中间`的 Excel 缓存值同步。
5. 除`可售月数`和`库存状况`外，以下字段均使用`SUMIFS`对结果表`SKU日销`同名列汇总，条件为`SKU日销 / 站点=当前站点`且`SKU日销 / 主SKU分类=当前主SKU分类`：`当日库存`、`月销量`、30/60/90天在途数量，以及全部30/60/90天货值、货值+海运、销售额、账单收入。
6. `可售月数=IFERROR(当日库存/月销量,0)`；`库存状况`沿用中间表口径置空。
7. 当前日期和所有历史`YYYY.MM.DD销量`均按相同粒度从结果表`SKU日销`同名日期销量列使用`SUMIFS`汇总，并保留公式。若`SKU日销`缺少同名历史销量列，则停止执行，不得沿用类目旧值。
8. 如果当前销量列不存在，必须与同日期涨幅列一起插入在`库存状况`后、所有历史日期列前，顺序为`YYYY.MM.DD销量`、`YYYY.MM.DD涨幅`。
9. 更新前先清理重复涨幅列和孤立涨幅列；每个销量列后必须紧跟同日期涨幅列。
10. 涨幅公式为`(该日期销量-右侧上一历史日期销量)/该日期销量`并使用`IFERROR`保护；最早、没有上一历史日期的涨幅置空。
11. 类目数据公式必须引用当前结果工作簿`SKU日销`，不得引用每日中间表或外部工作簿；公式逐行保留。
12. `产品主类/材质`由`类目日销数据口径`静态写入；新增销量/涨幅列表头继承相邻历史列样式，日期保持从左到右由晚到早。

### 4.5 生成或更新 `运营日销`

1. 来源：
   - `ROOT/数据中间表/每日数据中间表/每日数据中间表_D.xlsx / 运营日销数据口径`
   - `ROOT/数据中间表/每日数据中间表/每日数据中间表_D.xlsx / 运营日销-中间`
   - `ROOT/数据中间表/每日数据中间表/每日数据中间表_D.xlsx / 千易商品销售统计`
2. 结果表粒度：`站点 + 运营 + 主SKU分类`。
3. 结果表行已由`运营日销数据口径`预先生成和排序。对每一行 `运营日销-中间` 按 `站点 + 运营 + 主SKU分类` 查找结果行；未在数据口径表出现的指标行必须跳过并输出日志，不得追加新行。
4. 维度字段：`站点`、`产品分级`、`产品主类/材质`、`主SKU分类`、`运营`统一由`运营日销数据口径`写入。
5. 销售日期 `S` 对应的 `YYYY.MM.DD销量` 来自 `当前excel / 运营日销-中间` 同名字段经过 Excel 公式计算后的缓存值。
6. 如果该销量列不存在，必须插入在基础字段 `运营` 后、所有历史销量列前。
7. 结果表字段写入静态值或已计算缓存值，不允许写入外部工作簿引用公式。
8. `产品主类/材质`由`运营日销数据口径`静态写入，不得复制指标表的`XLOOKUP`公式或缓存结果。
9. 新增销量列表头必须继承相邻历史销量列表头样式和列宽。
10. 保留已有历史日期销量列，并保持从左到右由晚到早。

### 4.5.1 生成或更新 `C2运营店铺目标跟踪`

1. 来源：
   - `ROOT/数据中间表/每日数据中间表/每日数据中间表_D.xlsx / C2运营店铺目标跟踪数据口径`
   - `ROOT/数据中间表/每日数据中间表/每日数据中间表_D.xlsx / C2运营店铺目标跟踪-中间表`
2. 结果表粒度：`站点 + 店铺名 + 运营 + 主SKU分类`。
3. 结果表行由`C2运营店铺目标跟踪数据口径`预先生成和排序。对每一行 `C2运营店铺目标跟踪-中间表` 按同一粒度查找结果行；未在数据口径表出现的指标行必须跳过并输出日志，不得追加新行。
4. 维度字段：`站点`、`店铺名`、`运营`、`产品分级`、`产品主类/材质`、`主SKU分类`统一由`C2运营店铺目标跟踪数据口径`静态写入。
5. 销售日期 `S` 对应的 `YYYY.MM.DD累计销量` 来自 `当前excel / C2运营店铺目标跟踪-中间表` 同名字段经过 Excel 公式计算后的缓存值，写入结果表为静态值。
6. 历史 `YYYY.MM.DD累计销量` 列必须按日期从晚到早保留；本次新增的累计销量列必须插入到历史累计销量区域最左侧。
7. 结果表字段写入静态值或已计算缓存值，不允许写入外部工作簿引用公式。

### 4.5.2 生成或更新 `C1运营类目目标跟踪`

1. 来源：
   - `ROOT/数据中间表/每日数据中间表/每日数据中间表_D.xlsx / C1运营类目目标跟踪数据口径`
   - `ROOT/数据中间表/每日数据中间表/每日数据中间表_D.xlsx / C1运营类目目标跟踪-中间表`
2. 结果表粒度：`站点 + 运营 + 主SKU分类`。
3. 结果表行由`C1运营类目目标跟踪数据口径`预先生成和排序。对每一行 `C1运营类目目标跟踪-中间表` 按同一粒度查找结果行；未在数据口径表出现的指标行必须跳过并输出日志，不得追加新行。
4. 维度字段：`站点`、`产品分级`、`产品主类/材质`、`主SKU分类`、`运营`统一由`C1运营类目目标跟踪数据口径`静态写入。
5. 销售日期 `S` 对应的 `YYYY.MM.DD累计销量` 来自 `当前excel / C1运营类目目标跟踪-中间表` 同名字段经过 Excel 公式计算后的缓存值，写入结果表为静态值。
6. 历史 `YYYY.MM.DD累计销量` 列必须按日期从晚到早保留；本次新增的累计销量列必须插入到历史累计销量区域最左侧。
7. 结果表字段写入静态值或已计算缓存值，不允许写入外部工作簿引用公式。

### 4.6 保存结果表

1. 保存 `ROOT/结果表/每日销售及库存跟进表.xlsx`。
2. 不删除历史日期销量列。
3. 不删除新增粒度行。
4. 保存后必须调用本机 Microsoft Excel 全量计算结果表公式，等待计算完成后再次保存并关闭，使30/60/90天派生指标、类目汇总及涨幅公式获得最新缓存值。
5. 确保五个业务 sheet 均存在：
   - `SKU日销`
   - `类目日销`
   - `运营日销`
   - `C1运营类目目标跟踪`
   - `C2运营店铺目标跟踪`

---

## 五、输出校验

### 5.1 中间表校验

检查 `ROOT/数据中间表/每日数据中间表/每日数据中间表_D.xlsx`：

1. 必须包含 19 个 sheet：
   - `SKU日销-中间`
   - `类目日销-中间`
   - `运营日销-中间`
   - `C1运营类目目标跟踪-中间表`
   - `C2运营店铺目标跟踪-中间表`
   - `SKU日销数据口径`
   - `类目日销数据口径`
   - `运营日销数据口径`
   - `C1运营类目目标跟踪数据口径`
   - `C2运营店铺目标跟踪数据口径`
   - `千易商品销售统计`
   - `千易商品销售统计_累计`
   - `千易库存数据`
   - `千易入库单_新建`
   - `千易入库单_收货中`
   - `飞书-入库单跟进表`
   - `SKU信息表`
   - `店铺信息表`
   - `仓库信息表`
2. `SKU日销-中间`、`类目日销-中间`、`运营日销-中间`、`C1运营类目目标跟踪-中间表`、`C2运营店铺目标跟踪-中间表` 中的核心指标必须保留公式。
3. 30/60/90 天字段必须按本文档口径生成。
4. 置空字段必须保留字段列，不能删列。
5. `千易商品销售统计`、`千易商品销售统计_累计`、`千易入库单_收货中`、`飞书-入库单跟进表`、`千易入库单_新建`、`千易库存数据` 中的新增公式字段列必须使用浅蓝色背景，包含表头和公式数据单元格。

### 5.2 结果表校验

检查 `ROOT/结果表/每日销售及库存跟进表.xlsx`：

1. 必须至少包含 5 个业务 sheet：
   - `SKU日销`
   - `类目日销`
   - `运营日销`
   - `C1运营类目目标跟踪`
   - `C2运营店铺目标跟踪`
2. 必须存在销售日期 `S` 对应的销量列 `YYYY.MM.DD销量`。
3. `C1运营类目目标跟踪`、`C2运营店铺目标跟踪` 必须存在销售日期 `S` 对应的累计销量列 `YYYY.MM.DD累计销量`。
4. `类目日销` 必须存在销售日期 `S` 对应的涨幅列 `YYYY.MM.DD涨幅`。
5. `类目日销` 中所有历史日期销量列都必须存在同日期涨幅列。
6. 历史日期销量列、历史日期涨幅列和历史日期累计销量列必须保留。
7. 五张结果 Sheet 的粒度集合、行顺序和维度值必须分别与本次中间表的`SKU日销数据口径`、`类目日销数据口径`、`运营日销数据口径`、`C1运营类目目标跟踪数据口径`、`C2运营店铺目标跟踪数据口径`完全一致。
8. 结果表字段顺序应严格沿用已有结果表模板；若需新增同日期涨幅列，必须插入在对应日期销量列的相邻逻辑位置。
9. 结果表日期列必须从左到右由晚到早；本次新增销量列或累计销量列必须位于历史日期列最左侧。
10. 结果表不得存在包含 `#REF` 的公式，不得存在引用 `[1]#REF`、`[2]#REF`、`[3]...` 等不稳定外部工作簿链接的公式。
11. 结果表关键粒度字段不得为公式或错误值。
12. 结果表新增日期列表头样式应与相邻历史日期列表头一致；`类目日销` 的销量列和同日期涨幅列表头样式不得出现明显差异。
13. `SKU日销`、`运营日销`当前日期销量来自中间表公式缓存；`类目日销`当前及历史销量必须是引用结果表`SKU日销`的内部`SUMIFS`公式。
14. `C1运营类目目标跟踪`、`C2运营店铺目标跟踪`当前日期累计销量来自对应中间表公式缓存，写入结果表后必须为静态值，不保留外部引用公式。
15. `SKU日销`的供应商、四个单价和30/60/90天数量字段必须是中间表缓存静态值；十二个30/60/90天派生金额字段每个有效粒度行都必须是当前工作簿内部公式。
16. `类目日销`除`库存状况`外的全部基础数据字段和全部日期销量字段必须保留引用`SKU日销`的内部公式；`可售月数`必须为本行除法公式。
17. `SKU日销`与`类目日销`的`供应商`必须分别与对应中间指标表的 Excel 缓存值一致。

### 5.3 执行日志要求

脚本执行过程中需要用 `print` 输出清晰阶段日志，至少包含：

1. `源文件存在性与有效数据校验` 完成。
2. 每一个中间表 sheet 生成完成，输出 sheet 名、数据行数、字段数。
3. 中间表保存后，输出“开始调用 Excel 计算中间表公式”和“已完成 Excel 公式计算并保存中间表”。
4. Excel 公式计算后，输出 `SKU日销-中间`、`类目日销-中间`、`运营日销-中间` 当前销量字段的缓存可读取行数，并输出 `C1运营类目目标跟踪-中间表`、`C2运营店铺目标跟踪-中间表` 当前累计销量字段的缓存可读取行数。
5. 每一个结果表 sheet 更新完成，输出 sheet 名、数据行数、字段数。
6. 输出`SKU日销`供应商及四个单价同步完成日志，以及12个派生公式字段刷新行数；输出`类目日销`供应商同步行数。
7. 每个结果 Sheet 按中间表数据口径完成的全量粒度对齐结果，包含目标行数、新增数、移除数、位置变化数和维度更新单元格数。
8. `SKU日销`本次静态同步字段清空日志，以及`类目日销`内部公式刷新日志。
9. 中间表保存完成、结果表 Excel 全量计算完成、结果表保存完成。
10. 输出文件结构与关键字段校验完成。

---

## 六、可复用脚本或信息说明

1. 可复用 `数据中间表生成逻辑.md` 作为字段、sheet、公式和粒度逻辑的权威说明。
2. 可复用 `数据中间表数据流向图.html` 作为数据流向和引用关系的可视化说明，不作为执行脚本。
3. 可复用历史 `ROOT/数据中间表/每日数据中间表/每日数据中间表_YYYY-MM-DD.xlsx` 的 sheet 顺序和公式结构，但必须按本次执行日期重新填充数据和公式引用。
4. 可复用历史 `ROOT/结果表/每日销售及库存跟进表.xlsx` 的格式、列顺序和历史日期销量列。
5. 可复用 `ROOT/源数据/映射表/SKU、店铺、仓库映射表.xlsx` 中的三个维表。
6. 当前目录下 `飞书操作脚本/add_exchange_rate_record.py` 是汇率记录维护脚本，与本次结果表生成逻辑无直接关系，不建议复用为本流程执行脚本。
