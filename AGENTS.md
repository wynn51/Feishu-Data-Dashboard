# 项目协作规则

本文件适用于当前目录及其全部子目录。执行任何检查、文档维护、脚本开发或 Excel 处理任务时，必须遵守以下规则。

## 1. 项目目标

本项目用于维护以下完整数据链路：

1. 从 `源数据` 读取每日、月度原始数据及管理粒度、映射、人工维护数据。
2. 按中文逻辑脚本中定义的字段、粒度、顺序和公式生成 `数据中间表`。
3. 使用经过 Microsoft Excel 实际计算并保存后的数据中间表生成 `结果表`。
4. 保留 Excel 模板原有的 Sheet 名、字段名、字段顺序、公式、格式、历史数据和人工维护数据。
5. 当业务逻辑发生变化时，按用户指定范围联动维护中文逻辑脚本、Python 脚本和数据流向图。

业务规则的详细定义以项目内最新的每日、月度中文逻辑脚本为准。`AGENTS.md` 只定义长期稳定的执行边界和协作规则，不在此重复维护具体指标公式。

## 2. 根目录结构约束

包含本文件的目录为项目根目录 `ROOT`。允许操作的项目范围如下：

```text
ROOT/
├─ 源数据/                 # 原始输入、映射表、管理粒度和人工维护数据
├─ 数据中间表/             # 每日、月度中间表和在途批次表
├─ 结果表/                 # 每日跟进表、运营大盘等最终结果
├─ 代码/                   # Python 执行脚本
├─ AGENTS.md
├─ *中文逻辑脚本*.md
├─ *数据中间表生成逻辑*.md
└─ *数据流向图*.html
```

必须遵守以下边界：

1. 未经用户明确授权，禁止读取、修改、创建、移动、重命名或删除 `ROOT` 之外的文件。
2. 用户明确提供的根目录外附件可以只读检查；除非用户明确要求，否则禁止修改、覆盖或删除该附件。
3. `源数据` 默认为只读目录。未经用户明确要求，禁止修改、覆盖、重命名、移动或删除其中任何文件。
4. 只修改用户本次指定的文件类型和业务范围。用户要求“只更新文档”时，禁止同步修改 Excel、Python 或 HTML；用户要求“只检查”时，禁止修改任何项目文件。
5. 禁止因格式整理、重构或清理而改动无关文件、无关 Sheet、无关字段、历史数据或人工维护数据。
6. 禁止执行破坏性操作，包括但不限于递归删除、清空目录、覆盖源文件、重置整个工作区。
7. 临时分析文件只能放在 `ROOT/.codex_tmp/`，任务结束后删除本次创建的临时文件，不得误删其他任务或用户文件。
8. 生成的 Python 代码保存在 `ROOT/代码/`；业务逻辑文档和数据流向图沿用项目当前存放位置与命名方式，除非用户另有指定。

## 3. Excel 读取规范约束

### 3.1 默认读取方式

1. `.xlsx`、`.xlsm` 优先使用项目运行环境中的 Python 和 `openpyxl` 进行只读检查。
2. 中文路径必须作为命令行参数或函数参数传递给 Python，禁止将中文绝对路径直接写入通过管道传递的临时 Python 源码中。
3. 同一个关键工作簿应按需要进行两种读取：
   - `data_only=False`：读取公式文本。
   - `data_only=True`：读取 Excel 最近保存的公式缓存结果。
4. 读取时必须记录实际文件路径、文件修改时间、Sheet 名、数据行数和表头。不得仅凭文件名推断已读取正确文件。
5. Sheet 名必须按工作簿中的真实名称匹配，包括前后空格、全角半角符号及大小写差异；发现异常名称时应记录日志，不得静默改名。

### 3.2 读取失败时的降级顺序

当默认方式读取失败时，按以下顺序处理：

1. 检查文件是否存在、扩展名是否正确、是否被 Excel 或其他进程占用。
2. 对被占用或体积较大的 `.xlsx` 文件，使用只读 ZIP/XML 方式读取：
   - `xl/workbook.xml`：Sheet 名。
   - `xl/_rels/workbook.xml.rels`：Sheet 与 XML 的对应关系。
   - `xl/worksheets/sheet*.xml`：单元格、公式及缓存结果。
   - `xl/sharedStrings.xml`：共享文本。
3. ZIP/XML 读取文件时应使用允许 `ReadWrite/Delete` 共享的只读文件流，避免因为 Excel 占用而误判文件损坏。
4. `.xls` 不能使用 `openpyxl`。优先通过 Microsoft Excel COM 只读打开或转换后读取，禁止仅修改扩展名伪装成 `.xlsx`。
5. 大型工作簿不得把 `artifact-tool` 作为首选读取方式；若导入超时，应立即改用 `openpyxl` 或 ZIP/XML，不得无限重试。

### 3.3 读取结果校验

1. 读取成功不等于数据有效，必须校验目标 Sheet 存在、表头符合预期、有效数据行不为空。
2. 不能只根据 `max_row` 判断文件是否有数据，应检查有效数据区域中是否存在非空值。
3. 公式检查必须同时检查公式文本和缓存值，避免把未重新计算的旧缓存当成最新结果。
4. 读取方法发生降级时，日志必须说明失败原因、最终使用的方法和校验结果。

## 4. Excel 公式与计算规则

1. Excel 公式是业务逻辑的一部分。未经用户明确授权，禁止将应保留的公式替换为 Python 计算值或静态值。
2. 生成公式时必须按指标依赖顺序执行：先生成被引用 Sheet 和基础字段，再生成引用字段、汇总 Sheet 和结果表。
3. 禁止硬编码可能变化的列号。应优先按字段名定位列，再生成对应公式或数据引用。
4. 公式复制和填充必须保持绝对引用、相对引用、Sheet 名和字段范围正确，不得产生错行、错列或错误的历史月份引用。
5. 数据中间表生成完成后，必须使用 Microsoft Excel 打开该文件，执行全量计算，等待计算完成，保存并关闭，然后才能生成结果表。
6. 结果表读取的数据必须来自 Excel 保存后的公式缓存结果，不得用 Python 对同一指标重新计算后冒充 Excel 公式结果。
7. 在 Excel 重新计算完成前，不得使用 `data_only=True` 的值作为最终结果。
8. 必须扫描并报告 `#REF!`、`#VALUE!`、`#DIV/0!`、`#NAME?` 等非预期公式错误，报告具体到文件、Sheet、字段列和单元格。
9. `#N/A` 是否保留由具体业务逻辑决定；已明确要求保留的 `#N/A` 不得自动替换为空白或 0。
10. 禁止引入未经确认的外部工作簿引用。发现模板遗留外链时，应列出来源、位置和影响范围，等待用户确认处理方式。
11. 修改公式时必须保留目标模板原有单元格格式、数字格式、填充色、边框、条件格式和列宽，除非用户明确要求调整样式。
12. 如果无法调用 Microsoft Excel 完成真实计算，必须明确说明阻塞原因，不得声称结果已经过 Excel 公式激活。

## 5. 校准和修改流程

### 5.1 用户要求“检查”或“校准”时

1. 默认只读取和分析文件，不直接修改 Excel、文档、代码或 HTML。
2. 对比实际文件、现有中文逻辑脚本和 Python 实现，区分以下问题：
   - 数据来源或日期选择错误。
   - Sheet、字段名或字段顺序错误。
   - 数据粒度、行数或排序不一致。
   - 公式、引用范围或缓存值错误。
   - 格式、外部链接、历史列或人工字段被意外修改。
3. 输出问题清单，每一项尽量包含：文件、Sheet、字段、单元格或范围、实际情况、预期逻辑、影响范围和建议方案。
4. 对无法从现有文件确定的业务口径，单独列入“待确认清单”，禁止自行猜测并固化到代码。
5. 等待用户确认问题清单后，再执行联动修改；用户明确要求直接修复的情况除外。

### 5.2 用户确认修改后

1. 仅修改用户确认的事项和文件范围，不顺带处理未确认问题。
2. 业务逻辑变化应先落实到对应中文逻辑脚本，再同步 Python 代码；用户指定只修改其中一个文件时，以用户要求为准。
3. Python 代码修改完成后，应按用户给定日期实际运行；没有给定日期时，不得自行选择业务日期生成正式结果。
4. 每个 Sheet 或关键处理阶段完成后必须打印中文日志，包括输入行数、输出行数、公式数量、粒度变化和异常数量。
5. 执行完成后重新校准数据中间表和结果表，确认公式、缓存值、粒度、排序、历史列和保留字段均符合逻辑脚本。
6. 最终回复必须明确说明：修改了哪些文件、运行日期、输出文件、验证结果、尚未解决的问题和待确认项。

### 5.3 范围优先级

用户当前消息中的明确范围高于默认联动规则。例如：

- “只更新中文逻辑脚本”：只修改指定 Markdown 文件。
- “不要直接操作 Excel”：禁止修改或生成 Excel。
- “先给问题清单”：只检查并返回清单，等待确认。
- “同步调整中文逻辑脚本和 Python”：只联动这两类文件，除非用户同时要求更新 HTML 或 Excel。

## 6. 代码约束

**独立脚本**（`代码/*.py`）**不使用**显式类型注解（参数 `: type`、返回值 `-> type`、变量 `: type`），类型信息通过 docstring 描述。
**独立脚本**（`代码/*.py`）不使用 `logging` 模块，直接使用 `print()` 输出。
